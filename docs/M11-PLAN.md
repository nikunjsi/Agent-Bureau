# M11 plan: the Director

**Written:** 2026-09-22. **Owner:** Nikunj. **Status: OPEN.** Approved by Nikunj on 2026-09-22 with five changes, all applied: S1-11a, S1-11b, S1-7's hook-liveness check, E-2's key-file ACL, and corrected premises for S1-8 and S1-13.

§28's goal for M11 is one line: **"the product exists."** This is the closed
list of work that gets it there. It was built from nine named sources (A–I in
§S) and nothing else, the same way `docs/PRE-M11-PLAN.md` was. Three sessions:
§S1 makes the Director run and talk, §S2 takes a project from intake to an
approved plan, and §S3 takes an approved plan to a delivered result. The gate
at the end runs on the real engine, on a small real project.

---

## The rules

1. **The list is closed.** Sessions work what is written here and nothing
   else. No audits during M11. The scheduled post-M11 audit (M7–M11) and the
   all-three artifact regeneration happen **after the gate**, as their own
   steps.
2. **Something new turns up?** Add one line to **§F** with a proposed owner.
   Fix it now **only if both** are true: it would break M11, **and** it is in a
   file you are already changing for a listed row. Otherwise it waits.
3. **Don't widen a row beyond its "Done when".** A fix that grows is a new §F
   line.
4. **Method per row.** Write the failing test first and confirm it fails for
   the right reason. Fix it, then confirm it passes. **Standing rule 9:**
   before trusting a green run, show that the mutation really changed
   behaviour. One commit per row.
5. **Standing rule 1:** a behaviour or gate test drives the **real Director
   code** (the trigger queue, the context assembler, the real tool handlers
   over the real control channel). Scripted engine output from `FakeAdapter`
   is fine. A test that bypasses the Director's own code is not a Director
   test, and its name must say so.
6. **Standing rule 2:** every mechanism a row adds gets its **production
   caller in the same row**, or in a later row of the same session that names
   it. A row whose mechanism has no caller at session end is not DONE.
7. **Standing rule 6:** one decision, one place. The model is decided only in
   `Supervisor.assign()`. Intent is decided only in one classifier. Whether
   the brief is approved is decided only in one function
   (`isBriefApproved`, S2-3). Eligibility is decided only in one function
   (S3-2), which both the loop and `bureau_assign_task` call.
8. **Record progress in this file, in the same commit as the fix.** Set Status
   to `DONE <commit>`, `DECLINED: reason`, or `MOVED: owner, reason`. Don't
   save it for the end of the session: an earlier session lost hours that way.
9. **Running out of room?** Stop at a row boundary and commit. The next session
   re-reads this file and starts at the first OPEN row, using
   `/bureau-fix-session docs/M11-PLAN.md`. §S1 has 22 rows, so it may take
   two sessions.
10. **Stage files by name.** In `docs/artifacts/`, only `REGENERATION.md` and
    `README.md` are tracked: append to its pending list, never regenerate pages.
    **Never push.**
11. **One suite at a time. No `src/` edits while a packaged-app suite runs.**
    Report `test:security` as both runs.
12. **Spend (source I, a $5 prepaid key).** Everything that can run on
    `FakeAdapter` does. A real run is opt-in (`BUREAU_RUN_REAL_ENGINE_TESTS=1`),
    runs one at a time, and happens only in a row that names it. Its cost is
    written into that row's Status. Before any real run, Bureau's budgets are
    set per **E-7**. The gate is the main spend. Planned real runs:

    | Row | Run | Expected |
    |---|---|---|
    | S1-4 | `realEngineSpawn` + `realAgentGate` on the new pin | ~$0.20 |
    | S1-6 | `realEngineSpawn` with the key from the store | ~$0.05 |
    | S1-7a | the `--bare` proof | ~$0.15 (actual: **$0.0093**) |
    | GATE | a small real project, end to end | ≤ $3.00, capped by E-7 |

    If the running total passes **$4.00** before the gate, stop and ask Nikunj.

**Exit criterion: "the product exists."** All four must hold:
- **The gate passed on something real** (§GATE), and the record is written.
- Every row in §0 and §S1–§S3 has a resolved Status, and every §E decision is
  filled in.
- §G's sweep is green, and CI on GitHub is green after Nikunj pushes. **A red
  CI run is a plan row, not background.**
- §H's coverage check passes.

Then this file's status becomes CLOSED, `PROJECT-CHECKLIST.md`'s M11 row
becomes ✅, and the post-M11 audit and the artifact regeneration are scheduled
as their own steps.

---

## Where this plan differs from the suggested split

The prompt's split is kept, with three moves, each argued:

- **The conversation switcher moves from §S1 to §S2 (S2-1).** A second
  conversation exists only once a project is created from chat, and that is
  §S2's intake. In §S1 the switcher would be a control with one item, which is
  exactly why `NEXT-VERSION` §K.2 deferred it.
- **The Director's 19 tools are split by who first needs them**, not all in
  §S1: 7 in §S1 (S1-12), 4 in §S2, and 8 in §S3. Standing rule 2 needs each
  tool's caller to arrive with it, and `bureau_write_plan` has no caller in
  §S1.
- **Appendix B (the employee prompt, `NEXT-VERSION` §M.5) moves to §S3
  (S3-1).** Source B lists it beside the Director's slots, but Appendix B is
  the *employee* template, and it is only rendered when an employee gets a
  task, which is §S3. The Director's Appendix A.2 slots stay in §S1 (S1-17).

---

## §0: Housekeeping

| ID | What | Done when | Status |
|---|---|---|---|
| 0.1 | Commit this file as `docs/M11-PLAN.md` | One `docs:` commit | DONE fe77474 |
| 0.2 | Point `PROJECT-CHECKLIST.md`'s M11 row at this plan | The row says "in progress, `docs/M11-PLAN.md`" | DONE 7c7df63 |

---

## §S1: The Director runs and talks (session 1)

The first four rows are security fixes and the engine version. They come
first because S1-7's `--bare` check depends on the version, and the version
drift check fires falsely until S1-3 lands.

**Order note (2026-09-22, while working):** S1-4 closes **after** S1-5 and
S1-6. Its Done-when needs `realEngineSpawn` and `realAgentGate` to run for
real, and until S1-6 those tests sign in with Nikunj's subscription, which
E-4 rules out for Bureau-driven runs. S1-4 still precedes S1-7. Every real
run also needs the E-2 key file, `%USERPROFILE%.bureau-testanthropic.key`,
which Nikunj creates.

| ID | Source | What (plain) | Done when | Status |
|---|---|---|---|---|
| S1-1 | D (Known Issues 2026-09-22) | `readControlJsonAcl` matches English account names (`Everyone`, `BUILTIN\Administrators`, …) in `icacls` output. On a non-English Windows, a broadened ACL on `control.json` could pass verification | Verification compares **SIDs**: `S-1-1-0`, `S-1-5-32-545`, `S-1-5-11`, `S-1-5-32-544`, and the current user by SID (read from `icacls /save` SDDL, or the equivalent that prints SIDs). A failing test first: a parser test fed a localised listing (for example `Jeder`, `VORDEFINIERT\Administratoren`) passes the old check. Plus a real-file test that grants an explicit ACE **by SID** (`*S-1-1-0`), and verification refuses it. Mutation: an empty forbidden-SID set fails both. Known Issues row closed | DONE b341db6: `readControlJsonAcl` reads SDDL through `icacls /save` and compares trustee SIDs (aliases `WD`/`BU`/`AU`/`BA`/`SY` resolved, any other alias refused). The user's SID comes from `whoami /user` by absolute path, because Git's coreutils `whoami` sits on PATH and rejects `/user` (that is what failed first). The `icacls` call is injectable, so four new cases simulate a German machine: `Jeder` and `Administratoren` are refused (both passed the old check), a same-named account from another domain is refused, and SYSTEM plus the user passes. A real file granted `*S-1-1-0` is refused. Mutation (empty forbidden set) fails 4 tests. The older tests' English-name `raw` assertions now match SDDL |
| S1-2 | D (Known Issues 2026-09-22) | `tokenAcl.test.ts`'s "fails closed" test asserts `expect(true).toBe(true)` | The test makes verification fail for real, through an injected verifier or an ACE the tightening cannot remove, and asserts that `writeControlJsonWithAcl` throws **and** the file no longer exists. Mutation: removing the delete-on-failure fails it. Known Issues row closed | DONE b0c0373: two real tests replace the placeholder. The real `icacls` sets the ACL, and only the read-back is injected (`writeControlJsonWithAcl` passes S1-1's deps to verification only): one makes verification say no, the other makes it throw. **The second found a real gap:** a throw from `icacls` or from verification skipped the delete and left the token on disk, possibly with the directory's default ACL. Every way out that is not a confirmed ACL now deletes the file (one try/catch). Mutation (delete removed) fails both. The control-channel folder is green (124) |
| S1-3 | C (§F P-9) | Version drift compares `'2.1.238 (Claude Code)'` with the pin `'2.1.238'`, so `employee.engine_version_drift` fires on every spawn | `checkEngineVersionDrift` compares the **leading semver** of the reported string. Tested with the real CLI's exact output shape: the matching version gives no drift, a different one does. Mutation: going back to a raw-string compare fails it | DONE 5cc9840: claim checked first (the probe returns `--version`'s trimmed stdout, and this machine's CLI prints `2.1.276 (Claude Code)`). `checkEngineVersionDrift` compares the leading `major.minor.patch`, anchored so `2.1.2380` and `2.1.238.1` are not read as `2.1.238`. Output with no version is drift. The drift record keeps the CLI's own string. `tests/unit/engine/engineVersionDrift.test.ts` (4 cases): the pinned version in the CLI's real shape failed before the fix. Mutation (anchor removed) caught. The supervisor emission test and contract test 10 are still green |
| S1-4 | D (Known Issues 2026-09-22), E-1 | The dev box runs Claude Code 2.1.276, but `TESTED_ENGINE_VERSIONS` and CI pin 2.1.238 | **E-1 decided** (recommendation below). `TESTED_ENGINE_VERSIONS` and `.github/workflows/ci.yml`'s install step move **together**, as the Known Issues row requires. The contract suite runs green, and `realEngineSpawn` and `realAgentGate` run once opt-in on the chosen version, with their cost recorded here. Known Issues row closed. **Precedes S1-7** | DONE d12614e (code) + both real runs, 2026-09-23. `TESTED_ENGINE_VERSIONS` and `ci.yml` moved to **2.1.276** together (E-1), and `tests/integration/engine/engineVersionPin.test.ts` keeps them there: one free check that `ci.yml` installs exactly the pinned version, one that the `claude` installed on this machine reports a pinned version. The second failed for the right reason first (2.1.276 against the 2.1.238 pin); mutation: moving `ci.yml` alone fails the first. Contract suite green on 2.1.276 (31 passed, 3 opt-in skipped). **Both opt-in runs then passed on 2.1.276, through the real adapter built by `createClaudeCodeAdapterFromSettings` with the key from the secret store:** `realEngineSpawn` **$0.1356**, `realAgentGate` **$0.0672** — $0.2028 together, against E-7’s $1 daily / $0.25 per task, which the tests now set before they spend. `realEngineSpawn` cost more than the plan’s ~$0.05 estimate for it because the context carries no `modelId`, so the CLI runs its own default model rather than the `fast` tier; noted in §F. **The first attempt 400’d** (“this API key is not scoped to a workspace”) — the key, not Bureau, confirmed by a direct `/v1/messages` call with the same key, and nothing was billed. Nikunj replaced it with a workspace-scoped key and both runs went through unchanged. Known Issues row closed |
| S1-5 | H + §F (planning) | **The Anthropic API key field.** Settings needs a field for it, following `HelperKeyField`. **Correction to the prompt's premise:** `settings.setSecret` and `clearSecret` are `stub('M13')` (`ipc/handlers/settings.ts:104`), so today no key can be saved through IPC, including X-20's helper key. `storeSecret`/`clearSecret` in `secrets/secretStore.ts` exist (DPAPI via safeStorage) and are not wired | `setSecret`/`clearSecret` call `storeSecret`/`clearSecret` for an **allowlist**: `anthropic_api_key` and `oneshot.apiKey`. Any other key gets `VALIDATION_FAILED`. Each write emits exactly one event, and the payload names the key but never the value. `getSecretsStatus` reports both keys. Settings gains an "Anthropic API key" field that shows status only, never the value. Integration test through the real `dispatchIpcCall`: the key is stored, then `createRealSecretBroker(db).resolveForSpawn` returns `ANTHROPIC_API_KEY` and registers it for redaction. The value appears in no event, log line or IPC response (S4-style assertion). Renderer test for the field. X-20's helper field now saves for real (its test drives the real handler). Mutations: allowlist removed, and broker not registering for redaction | DONE cc3f701: the allowlist is a `z.enum` in the shared IPC schema (`STORABLE_SECRET_KEYS`), so any other name fails validation before a handler runs. The handlers call `storeSecret`/`clearSecret` through an injectable `ctx.safeStorage` (Electron's DPAPI when omitted). A machine without OS encryption is refused with the store's own plain reason. Each write emits one `app.setting_changed` naming the key only. `getSecretsStatus` lists both keys, and only when a value is stored. `HelperKeyField` became a generic `SecretKeyField` used twice: the new **Anthropic API key** field and the helper key, both in the Engines group. `tests/integration/ipc/secretsHandlers.test.ts` (7 cases, through the real `dispatchIpcCall`): stored encrypted, the value in no event, JSONL line or IPC response, the real broker injects it and registers it for redaction, clear, refusal of an unlisted name and of plaintext, and each name equals its reader's constant. Renderer tests for the field and its place in the panel. Mutations caught: allowlist loosened (1 fails), broker not registering (1 fails). **Also fixed here:** S1-1's commit had left the unit suite red, because its test title `(M11 S1-1)` read as a claim of security test S1 to `securitySuiteCoverage` (see §F) |
| S1-6 | H, E-2 | The opt-in real-engine tests copy Nikunj's **subscription** credentials into the employee's config directory and use `noopSecretBroker` | `tests/helpers/realEngineAdapter.ts` builds the adapter through `createClaudeCodeAdapterFromSettings(db)` (§F S-1's rule) and gets the key the way production does, per **E-2**: stored with `storeSecret` into the test's database, and injected only into the child by the real `createRealSecretBroker`. The credential copying is removed. **The key file's protection is enforced:** the helper refuses a key file whose ACL grants anyone but the current user, reusing `control.json`'s verification (SID-based after S1-1). It never logs or echoes the key, and it is a dev-only path that is never shipped. A test shows a broadened key file is refused. The helper **refuses to run** if `process.env.ANTHROPIC_API_KEY` is set in the parent, and a free unit test asserts that nothing under `tests/` assigns or reads `ANTHROPIC_API_KEY` except that refusal. `realEngineSpawn` runs once opt-in, with its cost recorded | DONE 0f86fa7 (code) + the opt-in run, 2026-09-23. `tests/helpers/realEngineKey.ts` refuses a parent `ANTHROPIC_API_KEY` (read through an injectable `env`, so no test touches `process.env`), refuses a key file not restricted to the user by the SID check, stores the key with `storeSecret` behind a test stand-in for DPAPI, and returns the real `createRealSecretBroker`. The adapter helper builds through `createClaudeCodeAdapterFromSettings(db)`. `realEngineKey.test.ts` (5 cases, real files and a real ACL read) and `realEngineKeyHygiene.test.ts` (3). Mutations caught: the ACL check removed (2 fail), the helper reading `process.env` directly (hygiene fails). **The remaining run is done:** the E-2 key file exists, its ACL verifies, and `realEngineSpawn` reached the real API with the key taken from the store and injected into the child by the real broker — the credential copying is gone for good. **Cost $0.1356** (the same run S1-4 records; it was made once, not twice). The file’s first key was org-scoped and 400’d before billing anything; the workspace-scoped replacement works |
| S1-7a | H, B15 (§M11 item 15) | **`--bare`, measured, and what follows from it.** Claude Code is making `--bare` the default for `-p`. It uses only an API key, and it skips things like hooks and discovery, so the policy hook might stop running (split from S1-7, 2026-09-23: the measurement and its consequences are one commit, the per-session liveness check is another — the same shape as S1-12a/b) | An opt-in real test runs the real adapter with `--bare` and proves (a) Bureau's MCP server loads, (b) whether the `--settings` PreToolUse hook fires, (c) the adapter's own `--settings`/`--mcp-config` files are the ones used. **If all three hold,** `--bare` is passed whenever the broker yields a key. **If the hook does not fire, `--bare` is never passed** (invariant #6), recorded in §7.6 and on risk #34. Either way, per risk #34's decision E-4a: with no key stored, `assign()` refuses a claude-code employee with a plain-language `UserFacingError` naming the Settings field (test). Cost recorded | DONE c85ca97: **measured, and the answer is no.** `tests/contract/realBareMode.test.ts` (opt-in) builds the ctx, the launch spec, the config files and the turn argv from the real adapter (`buildLaunchSpec` + `buildTurnArgs`, the two functions production spawns from), adds `--bare`, and spawns it. On 2.1.276: **(a) holds** — the model called `mcp__bureau__bureau_report_status` and the Core recorded `employee.status_reported`; **(b) fails** — no `tool.requested` at all, so the PreToolUse hook never ran; **(c) holds** — the `--settings` and `--mcp-config` paths in the argv are exactly the spec’s two config files. **So a `--bare` session changes Bureau’s state with no policy check, and `--bare` is never passed** (invariant #6). `bareModeAndKeyRequired.test.ts` is the free guard: neither the spec nor the turn argv may contain it, nor `CLAUDE_CODE_SIMPLE` in the env. Recorded in §7.6 (a table of what survives `--bare`) and on risk #34. **And E-4a’s refusal:** `Supervisor.assign()` now throws `EngineApiKeyRequiredError` (a `UserFacingError`) before any spawn when the adapter is really `claude-code` and no key is stored, naming Settings → Engines → Anthropic API key. Keyed on the **adapter**, not `employee.engine`: the question is whether a real CLI is about to launch, so every FakeAdapter test is untouched. `isAnthropicApiKeyStored` reads `secrets_meta` only — metadata, never the value (§11.4). **And the refusal reaches the person who can act on it:** `main()` calls `startDirector` unawaited and its own `.catch` only reaches the terminal, so a thrown refusal would be invisible to the one person who can fix it. `startDirector` now returns `not_configured` for a `UserFacingError` from `assign()`, and `reportDirectorStart` writes it into the chat as one plain system message (standing rule 2: the mechanism has its production caller in the same row). Two existing suites now satisfy the same precondition production does, through a new `storeTestAnthropicKey` helper — which is evidence the guard is on the real path, not beside it. Mutations caught: the key check disabled (1 fails), `--bare` added to the spec (1 fails), the refusal rethrown instead of reported (1 fails). **Cost $0.0093** for the measurement (run on Haiku deliberately — whether the CLI runs a hook is not a property of the model), plus $0.00 for a second, free measurement with a deliberately invalid key that showed `SessionStart` fires without `--bare` and not with it. **Note on numbering:** the “E-4” this row cites is the risk register’s decision E-4 (risk #34, API key not subscription), not §E’s E-4 |
| S1-7b | H, B15 (§M11 item 15), split from S1-7 2026-09-23 | **Hook liveness: the Core confirms the policy hook actually ran for this session, rather than trusting that it was registered.** Not passing `--bare` stops protecting anything once the CLI makes it the default for `-p` — S1-7a measured that an ungoverned session still reaches Bureau and changes its state | Before a session is treated as governed, the hook must have reached the control channel with that employee’s token. **Measured (S1-7a, free): the CLI does fire a `SessionStart` hook** — before any model call, carrying `session_id` — and a session started with `--max-turns 0` fires it with **no model call at all**, so the handshake costs nothing per employee. So: the adapter registers `SessionStart` alongside `PreToolUse`, the hook reports liveness, and the Supervisor assigns no work until it arrives. If it never arrives, it fails closed with one plain-language error. Tests: a launch whose hook config is removed or ignored is refused before any tool runs; a normal launch passes. Mutation: skipping the liveness check lets the broken launch through | DONE 5b7716c, 2026-09-24. The adapter registers the same `bureau-hook` script for `SessionStart`. On that event it only reports, to a new `POST /v1/hook/session-start`, with the employee's token (§7.10 amended, §0.1 row), then exits 0 and prints nothing. Where the engine gates by hook (`capabilities.hookInterception`), `Supervisor.assign()` runs `adapter.runHookHandshake(ctx)` before `transition('starting')` and waits for that report. No report → `EngineHookNotRunningError`, a plain-language `UserFacingError`, with no state change and so no event, like the other pre-spawn refusals; `startDirector` already writes such an error into the chat. An adapter that claims a hook and has no handshake is refused too. The session the hook reported is on `employee.started` as `hookSessionId`. **The row's premise was wrong, and the handshake is built around that** (§F): measured on 2.1.276, `--max-turns 0` still reaches the model step, and `continue: false` from a SessionStart hook doesn't stop it. So the handshake gets **no credentials** (the broker is never asked) and an `ANTHROPIC_BASE_URL` on a closed loopback port. The CLI fires `SessionStart`, then stops locally at "Not logged in". It is free whether or not the hook runs. `hookLiveness.test.ts` (3 cases, free) drives the real adapter, its real settings file, the real bundled hook, the real control channel and the real Supervisor. The one stand-in is the CLI, `fakeClaudeCli.cjs`, run through a new `spawnProcess` seam; it runs whatever `SessionStart` hooks the settings file names. A CLI that ignores hooks, and one whose hook config was removed, are both refused, with status `off`, no `employee.started`, and exactly one launch. A normal launch passes, and its handshake env has no key even though the broker holds one. Red first: both broken launches came back with `refusal` `null`. **Mutations, each with the value that changed:** the check skipped → `refusal` `null` for both broken launches and `hookSessionId` null (3 fail); the handshake handed the broker's key → `ANTHROPIC_API_KEY` = the test key (1 fails). **On the real CLI, opt-in:** `tests/contract/realHookLiveness.test.ts` passed through the whole production chain. The hook reported session `bd7b2651…`, `assign()` took 2.4 s, and there were 0 usage rows. **Cost $0.00**: no key, an unreachable API, and a deliberately invalid stored key, so E-7's S1 budgets were set but never approached. An earlier attempt at that run, with the key still injected, took 61.8 s and was killed at the deadline. That is how the `--max-turns 0` premise was caught, before any valid key saw it. `engineUninstalledMidSession.test.ts` has no control channel, so it now satisfies the precondition with a handshake stand-in, and says so in its comment. Spec §7.6, §7.10 and risk #34 updated. Suites, one at a time: unit 1,030 · integration 989 (150 files, freshly packaged; `assertPackagedAppIsNotStale` fired by name on a touched `src/` file, mtime restored) · contract 31 (+7 opt-in skipped) · `test:security` run 1: 97, run 2: 116 |
| S1-8 | B2 (§M11 item 2), C (§F S-1), `NEXT-VERSION` §H.6 | **The first production Supervisor is the Director's.** Nothing in `src/` calls `spawnSupervisedEmployee`. `new ClaudeCodeAdapter(` appears three times in `src/`: two probe-only sites (`zeroCostMode.ts:147`, `index.ts:244`) and the factory itself (`claudeCodeAdapter.ts:971`) | `startDirector()` in main runs this chain: the hired Director → an adapter from **`createClaudeCodeAdapterFromSettings(db)`** → `spawnSupervisedEmployee` → registered in the one `SupervisorRegistry` → `assign()` with a Director context (no task, no worktree). **It runs at startup, not lazily:** §8.0 calls the Director "the only always-warm agent process", and the restart report (S1-20) needs a live Director at boot. Startup costs nothing, because structured mode spawns one `claude -p` per turn and no turn runs until a trigger arrives. An integration test drives **`startDirector` itself**, with the engine swapped at the factory seam for `FakeAdapter`. It asserts the registry holds the Supervisor, the policy evaluator reads its **live** capabilities (N-3), and `runShutdownSequence` stops it (D-2). A unit test lists every `new ClaudeCodeAdapter(` in `src/`, and anything beyond the two probe sites and the factory fails. An engine without `mcpServers`+`sessionResume` gets a plain startup message instead of a Director (§8.0), which is tested. Mutation: `startDirector` not called from `main()` fails the test | DONE 94585ae: `src/main/director/startDirector.ts` hires nothing and decides no model. It finds the hired Director, builds the adapter through `createClaudeCodeAdapterFromSettings(db)` (a `createAdapter` seam for tests only), calls `spawnSupervisedEmployee`, and `assign()`s a context with no task and `worktreePath: ''`. An engine without `mcpServers` and `sessionResume` is stopped and reported in plain words, written into the chat by `reportDirectorStart` when a conversation exists. `main()` calls it after the control channel starts, not awaited. `startDirector.test.ts` (6 cases): registered with live capabilities, stopped by the real `runShutdownSequence` (row reads `off`), no Director → nothing, unsuitable engine → message and nothing running, the chat message, idempotent. `directorWiring.test.ts`: `main()` calls it without the seam, and `new ClaudeCodeAdapter(` appears only at the two probe sites and the factory. Mutations caught: the call in `main()` replaced, the capability check removed (2 fail). No e2e fixture seeds a Director, so no packaged test starts a real one. `NEXT-VERSION` §H.6 half resolved |
| S1-9 | C (§F P-10) | `containProcess()` has no production caller, so an engine's detached helpers are not in Bureau's Job Object | Every engine child either adapter spawns (each per-turn `claude -p`, and each PTY) is passed to `containProcess(pid)` immediately after spawn. If containment fails, the child is killed and the turn fails with a translated message (invariant #6). A unit test uses an injected contain function to show it is called for every spawn and that a failure kills the child. Mutation: the call removed fails it | DONE 4434245: `engine/containEngineChild.ts` contains a pid through an **injected** `containProcess`, because the Job Object addon is built for Electron's ABI and would break every plain-Node test that imports an adapter. Both adapters call it right after spawning (claude-code per turn, generic-pty per session). On failure the process is killed and the turn ends `finished`/`error` with a plain sentence, without flushing the queue. `startDirector` **requires** `containProcess` and passes it to the settings factory; `main()` passes the real one. `engineContainment.test.ts` (3 cases, real adapters with Node as the engine): the pid is contained, and a failure kills a process that would otherwise live a minute, with no queued send launched. `directorWiring.test.ts` checks `main()` passes the imported function itself (a no-op stand-in with the same name was first missed, then caught once the check was tightened). Mutations caught: claude-code call removed (2 fail), generic-pty call removed (1 fails), `main()` passing a no-op. Engine and Director integration folders green (131). `tests/helpers/adapterContext.ts` shares the real-adapter context with later rows |
| S1-10 | C (§F N-1) | The adapter's `exit` handler flushes a queued send after a park, which launches a new billed turn the Supervisor then ignores | The flush asks the Supervisor first: the adapter gets a `mayDeliver()` callback at start, and the Supervisor's state is the one place this is decided. A park or pause drops the queue, with the dropped count in the park event. Tested on the **real `ClaudeCodeAdapter`**, in the §7.4 real-adapter queue-test pattern (standing rule 1): queue a send, park, let the child exit, and assert that no second spawn happens. Mutation: an unconditional flush fails it | DONE 4bcf6bf: `EngineAdapter` gains `setDeliveryGate` and `dropQueuedSends`, implemented by all three adapters. `assign()` hands the adapter a gate that reads the Supervisor's own state, so "may we spend now" is not a second copy inside the adapter, and a flush while parked, stopping, off or failed is refused. `transition()` is the one place a park drops the queue — budget, quota, breaker and a user pause alike — and the count rides on the `employee.parked` event as `droppedSends`. `queuedSendAfterPark.test.ts` (3 cases): the real `ClaudeCodeAdapter` launches nothing on exit while the gate is closed, `dropQueuedSends` reports and clears, and a real Supervisor pausing mid-turn records `droppedSends: 2`. Mutations caught: the gate check removed, and the drop replaced by 0. Engine folder green (128), unit 1,024, messages and chat 85 |
| S1-11 | A1, §26 #1/#8 | **DirectorSession: persistent, resumed by `session_id`, no worktree.** Today `employees.session_id` and `conversations.director_session_id` are written only at insert, so resume is lost on restart | On `session.started`, the id is written to `conversations.director_session_id` (and `employees.session_id`) with one event. After a restart, the next turn passes `--resume <id>`. Tested with a new Supervisor in a restart-shaped integration test, plus a `buildLaunchSpec` unit test. `worktree_id` stays null and `cwd` is the state directory. A resumed session whose id the engine rejects starts fresh, seeded as in S1-18, and emits `director.session_restarted` | DONE 0299d0e: the Supervisor writes `employees.session_id` when the engine reports the session it started, and `assign()` resumes that id on the next start when the engine supports it. A refused resume clears the column and emits `director.session_restarted`. **One deviation from the row, argued:** the id is written to `employees.session_id` only, not also to `conversations.director_session_id` — two writers of one value is standing rule 6's shape. Compaction writes that column when it starts a fresh session (S1-18), which is the change it records. **No event of its own** either: the id rides on the `employee.idle` transition it causes, so one state change keeps one event (invariant #3). `directorSessionResume.test.ts` (3 cases: recorded, resumed across a restart with a new adapter and registry, refused → fresh plus the event) and `claudeTurnArgs.test.ts` (argv carries `--resume <id>`, through a new `buildTurnArgs` seam so no spawn is needed). Mutations caught: the write removed (3 fail), resume never asked (2 fail) |
| S1-11a | Standing rule 1 (Nikunj's review, 2026-09-22) | **A real tool-call bridge for FakeAdapter.** FakeAdapter only emits scripted events. With the real engine, a tool call goes CLI → the `bureau-tools` MCP server → the control channel → the handler, and a scripted FakeAdapter tool call reaches none of that. Without a bridge, S1-12, S1-13, S2-7 and S3-11 would either bypass the real handlers (standing rule 1's exact failure) or need something that does not exist | A FakeAdapter scripted tool call is delivered **through the real control channel**, the way `bureau-tools` sends it: real HTTP, the employee's real token, the real handler, and its real result fed back into the scripted turn. Built on how `tests/integration/checkpoints/m8Gate.test.ts` and `controlChannel/bureauToolsMcp.test.ts` drive the real path. Test: a scripted Director turn calls `bureau_report_status`, the Core records `employee.status_reported`, and the result is visible to the next scripted step. Mutation: a handler that throws must come back as the tool's error inside the turn, not be swallowed. **S1-12, S1-13, S2-7 and S3-11 use this bridge** | DONE e61c246: `tests/helpers/bureauToolBridge.ts` makes the same request `resources/bin/bureau-tools.ts` makes — loopback HTTP, the employee's real bearer token, a fresh idempotency key — and returns the reply for the next scripted step. `bureauToolBridge.test.ts` (3 cases) drives a real `startDirector` Director: `bureau_report_status` sets `status_detail` and emits `employee.status_reported`; a handler that throws returns a structured `INTERNAL_ERROR` and the channel keeps serving the next call; a bad token is refused. **One assertion corrected while writing it:** the first draft demanded the thrown message be hidden from the agent, which is wrong — §7.9 wants a structured error the *agent* can act on, and CLAUDE.md's translate rule is about the *user*. Mutation: the bridge short-circuiting instead of using the channel fails all 3 |
| S1-11b | §8.0 (`Read(${project}/**)`), E-3, Nikunj's review 2026-09-22 | **The Director's `${project}` is empty today.** `controlChannel/policy/contextBuilder.ts:63` derives `${project}` from the employee's **worktree**, and the Director has none, so for it `${project}` is `null`, which matches nothing. `Read(${project}/**)` would deny every read: the Director would be safe but blind, and E-3's attachments depend on the same thing | `${project}` resolves for the Director from its **active project** (the conversation's project) through one resolution function in `contextBuilder`, not a second copy (standing rule 6). It stays `null` when the Director has no active project. Tests through the real policy evaluator: the Director can `Read` a file inside its active project, is denied one outside it, and with no active project a read is denied. Employees still resolve from their worktree, unchanged (test). Mutation: removing the project lookup makes the inside read fail | DONE b2d11a6: `resolvePolicyProject` in `contextBuilder.ts` is the one function: an employee's project is its worktree's, the Director's is its conversation's, and null when it has none. `directorProjectVariable.test.ts` drives the real `/v1/policy/check` endpoint with the real evaluator against a Director started by `startDirector`: a file inside its active project is allowed, one outside is denied, and with no active project the read is denied. Employees are unchanged, proven by `policyRealEvaluator.test.ts` still green (32). Mutation caught: the Director branch removed fails the inside read |
| S1-12a | §7.9, B5, §26 #2/#10 (split from S1-12, 2026-09-22) | **The Director's tool surface**: its own handler set, who may call what, what the model is offered, and what `bureau-tools` advertises. Plus `bureau_report`, the one Director-only tool row S1-13 needs | `DIRECTOR_TOOL_HANDLERS` holds the Director's tools (`bureau_report` new; `bureau_raise_checkpoint`, `bureau_send_message`, `bureau_read_memory` shared with employees, not copied). The control channel picks the set from **who is calling**, and a tool belonging to the other role is refused with a `security` `control.authorization_rejected`, while an unknown name still answers `NOT_IMPLEMENTED` with no security event. `control.json` carries `isDirector`, because `bureau-tools` is spawned by the engine CLI and has no other way to know. The adapter's `--allowed-tools` follows the same function, so the Director is offered `Read`/`Grep`/`Glob` plus its own tools and **no `Write`, `Edit` or `Bash`** (§8.0). Tests: `directorToolSurface.test.ts` (4, both refusals with their events, the unknown name, and both `control.json` files), `directorAllowedTools.test.ts` (3, including that every advertised Director tool is implemented). Mutations caught: one shared set for everyone (2 fail), the Director offered the employee list (1 fails) | DONE 4c97c2a |
| S1-12b | §7.9, B5, B10, `NEXT-VERSION` §M.4 (split from S1-12, 2026-09-22) | **Director tools, batch 1 — the remaining handlers:** `bureau_get_project_state`, `bureau_write_memory`, `bureau_search_workspace` (the surface, and `bureau_report`, landed in S1-12a) | The `bureau-tools` MCP server advertises a Director definitions array when its control file names a Director. The adapter's `--allowed-tools` for the Director is its tool set plus `Read(${project}/**)`, `Grep` and `Glob`, with no `Write`, `Edit` or `Bash` (unit test on `buildLaunchSpec`). **Authorization:** a non-Director token calling a Director tool is refused, with a `security` `control.authorization_rejected`, and so is the reverse for employee-only tools (`bureau_task_done`). Each tool has Zod validation and a handler-level test over the real control channel, and its scripted Director-turn test calls it through S1-11a's bridge. `bureau_write_memory` writes `project` scope directly and asks for `company` through `memoryScopeRequiresApproval`, confined by `memoryTarget.ts`, with a junction-escape test. `bureau_search_workspace` is confined to the project root by canonicalising, and fails closed, with a junction-escape test **at the handler** (invariant #5: policy never checks `bureau_` tools). Invariant #5's list of guards in `CLAUDE.md` and §21 is updated. §M.4 is resolved for the tools in this batch | DONE 25893ee: `bureau_get_project_state`, `bureau_write_memory` and `bureau_search_workspace` are in `DIRECTOR_TOOL_HANDLERS`, so `--allowed-tools` and the authorization check pick them up from the one map S1-12a built; `bureau-tools` advertises all seven, and the advertised-equals-implemented test holds. **The two guards, at the handler:** `bureau_write_memory` goes through the same `proposeMemoryWrite` an employee uses, so `memoryTarget.ts` confines it; `bureau_search_workspace` is a new guard (`src/main/workspace/searchWorkspace.ts`) that canonicalises every entry against the project root before descending into or reading it, with caps on results, file size and files walked. Invariant #5's list in `CLAUDE.md` and §21 gains it, with a §0.1 amendment row. **Who the project is** is decided in one place for the Director (`resolveDirectorProject`), which `contextBuilder` now calls too, and **whether a scope is gated** stays in `memoryScopeRequiresApproval`, which takes the writer and answers for both (standing rule 6, `NEXT-VERSION` §M.4 resolved for this batch). `directorToolsBatch1.test.ts` (10 cases over the real control channel): project state with tasks/spend/blockers, both fail-closed refusals when there is no project, a direct `project` write, a queued `company` write with its checkpoint, a junction out of the memory tree, grep with a glob, `max_results` with `truncated`, a junction out of the project, and all three refused for an employee with a `control.authorization_rejected` each. Mutations caught: the entry-level containment removed (the junction test then reads a private key), the Director's free-`project` branch removed, and the memory tree's canonical check removed (2 fail). **Also:** the §F S1-5 practice was broken by this row and by S1-4 — a row ID in a `describe` title reads as a security-test claim, and the unit suite was red until both titles moved the ID into a comment |
| S1-13 | `NEXT-VERSION` §K.1, §26 #11, CLAUDE.md "translate" | **The Director's producer: its output into the chat.** `appendChatMessage` already has production callers (`checkpoints/surfacing.ts`, `ipc/handlers/chat.ts`, `messages/router.ts`), so no new caller is needed there. What is missing is **the Director's producer**: `ChatStreamRegistry` is constructed at `index.ts:275`, and nothing ever calls `begin()` | A Director turn's prose streams into the active conversation as `author: director`, through `ChatStreamRegistry.begin`, with `chat.stream_started`/`stream_completed`. Tool calls and tool results are **never** written to chat. `bureau_report` posts `report`/`summary` kinds. The "typing…" indicator shows while the turn streams. A FakeAdapter-scripted turn (text, a tool call through S1-11a's bridge, more text) appears in `chat.listMessages` as the prose only. Mutation: tool output let through fails it. §K.1 resolved | DONE 13cf751, 2026-09-24. `createDirectorChatProducer` (`src/main/director/directorChatProducer.ts`) turns the Director's `text.delta`s into one streamed `author: director` message per turn, through `ChatStreamRegistry.begin`, into the active conversation. It gets its events from a new Supervisor observer, `onAgentEvent`, called before the parked gate; a throw from it is contained. `startDirector` attaches it when given `chatStreams`, and `main()` passes its one registry (the production caller, standing rule 2). `directorWiring.test.ts` fails if that argument is dropped. Tool calls, their results, thinking and raw bytes are never written; prose that resumes after a tool call starts a new paragraph. A turn that says nothing writes no message. `turn.completed`, `idle` or `finished` completes the reply; `finished` with an error marks it `aborted`; a reply the user stopped stays stopped for the rest of that turn. **Redacted on the way in**: each reply has its own `RedactionStream`, flushed before completion (§11.4; the chat is an output path). `directorChatProducer.test.ts` (2 cases) drives the real `startDirector` and real Supervisor, with a real `bureau_report` call over the control channel through the bridge, and reads back through the real `chat.listMessages`. The chat holds exactly the prose, `Let me check where things stand.\n\nI posted the report. Your key «redacted:secret» is safe.`. Neither the tool's argument sentinel nor its result sentinel appears anywhere, the report is its own card, the row reads `streaming` mid-turn (what "typing…" renders from), and there is one `chat.stream_started` and one `chat.stream_completed`. The second case: a tools-only turn writes nothing, and an engine error leaves `Half a thou` marked `aborted`. Red first: both timed out waiting for a stream that nothing produced. **Mutation** (tool results written): the body became `Let me check where things stand.TOOL-RESULT-SENTINEL…`, and both cases fail. `bureau_report`'s cards were already built (S1-12a). One gap found: they are not pushed live (§F). §K.1 resolved. Suites, one at a time: unit 1,030 · integration 991 (151 files, freshly packaged) · `test:security` run 1: 97, run 2: 116 |
| S1-14 | A5, §26 #8 | **The Appendix A.3 state machine, persisted** in `conversations.director_state`/`director_state_data` (the columns exist; nothing writes them) | A.3's transitions are one table in code, and an invalid transition is refused. Each transition emits one event, using an existing type where one fits, or a §5.2 amendment plus a `§0.1` row where none does. A restart resumes mid-intake: the test restarts and asserts the state and that the next turn's context carries it. Mutation: the state not persisted fails it | DONE 5ce9de6, 2026-09-24. `src/main/director/directorState.ts`: `DIRECTOR_TRANSITIONS` is A.3's diagram as one table (19 arrows), and `transitionDirectorState` is the one way the state moves. It checks the table, writes `director_state` and `director_state_data` in one statement, then emits the arrow's one event. A missing arrow throws `InvalidDirectorTransitionError` with nothing written. `IDLE → INTAKE`, `SUPERVISING → ESCALATING` and `SUPERVISING → REPLANNING` use `director.intake_started`, `director.escalated` and `director.replanned`, which had no emitter until now. Every other arrow is the new `director.state_changed` with `from`, `to` and the trigger (§5.2 amended, §0.1 row, `check:event-taxonomy` green at 144). The shared `director_state` schema is now that closed enum, not a free string. **The next turn's context:** `describeDirectorStateForContext(conversation)` is the sentence the turn is told. S1-17's assembler renders it (named here, so its caller arrives in this session). The transitions' first production caller is S1-16 (new work → `INTAKE`), which names this row. `directorStateMachine.test.ts` (4 cases): new is `IDLE`; every arrow is allowed, persisted and emits exactly one event; `IDLE → PLANNING` is refused with the column still null and no `director.*` event; and after a real close and reopen of the database the state is `INTAKE` with `{ intakeRound: 2 }`, the context sentence names both, and `DRAFTING_BRIEF` follows from there. Red first: the module did not exist. **Mutation** (the write skipped): after the restart the state read `IDLE` instead of `INTAKE`, and 2 fail. **Suites, and one broken rule:** typecheck, lint, the four `check:` scripts and unit 1,030 were green on this row's tree. The packaged integration run started for it was **invalidated**, because S1-15's `src/` edits were made while it ran (rule 11), so it was stopped. The full integration suite runs once over S1-14 and S1-15 together, and its result is recorded on S1-15 |
| S1-15 | A4, B6 (§M11 item 6), §26.1 | **The trigger queue**, with coalescing. Never a bare timer | One queue decides every Director turn. The message router's delivery to `director` becomes an enqueue. Priorities follow §26.1. Triggers that coalesce do so within `director.coalesceWindowSeconds`. A user message is immediate and never coalesced with anything else, **but it waits for `idle`** (never inject mid-generation), and **several user messages that arrive while the Director is mid-turn become one turn** (§M11 item 6, from the parking lot). The heartbeat enqueues only if new events exist since the last report (`reporting.heartbeatMinutes`). §S1 wires the producers that exist now: user message, blocking checkpoint answered, `bureau_ask_director`, heartbeat, restart. §S2/§S3 rows wire theirs. Fake-clock tests cover each rule. Mutations: no coalescing, a bare-timer wake, and mid-turn injection each fail. The settings lose `inactiveUntil` | DONE 78facf5, 2026-09-24. **The rules** are in `src/main/director/triggerQueue.ts`: `DIRECTOR_TRIGGER_RULES` is §26.1 as one table, and `DirectorTriggerQueue` has an injected clock, idle check and delivery. It sends only while the Director is idle. Every waiting user message goes together and with nothing else; an answered blocking checkpoint and a restart go alone; coalescing triggers wait out `director.coalesceWindowSeconds` from the oldest and then go as one turn; priority picks among what is ready; a key offered twice is one trigger. `startDirectorHeartbeat` offers a trigger only when the newest event is past the one its last beat covered. **The wiring** is `src/main/director/directorTriggers.ts`. The message router now *offers* anything addressed to the Director (`director`, or `employee:<its id>`) instead of delivering it (`RouterReport.offeredToDirector`). The queue sends the turn through a new `Supervisor.deliverDirectorTurn`, then marks each message delivered with its `message.delivered`, as the router did. `checkpoints.answer` offers an answered `blocking` checkpoint that the Director did not raise; the Director's own arrives as its answer message. The heartbeat's "news" excludes the Director's own events and the chat, director and message traffic, so its own turn is never the reason for its next. `startDirector`'s observer tells the queue when each turn ends. `main()` builds one queue and passes it to the router, IPC and `startDirector`, and stops it on quit. Producers wired in §S1: user message, blocking checkpoint answered, `bureau_ask_director` (a Director-addressed `question`) and the heartbeat. **The restart producer is S1-20's**, which names this queue. **Two Supervisor defects fixed here** (§F): a structured Director never left `starting`, and a task-less turn ended `blocked`, so no message could ever reach it. Both fixes apply **to the Director only**: the first full run showed an existing test holding employees to M4's `ended_without_report`, which still holds for them. **The same run found a consequence:** a Director already idle when its first session starts made S1-11's `employee.idle { sessionId }` a same-state no-op, so the session id was written with no event. `transition()` now takes `evenIfUnchanged` for exactly that, and `directorSessionResume.test.ts` is green again. **Also aligned:** FakeAdapter now ends a turn on `finished`, as `ClaudeCodeAdapter` does on child exit. **Tests:** `triggerQueue.test.ts` (9, fake clock) covers each rule and the heartbeat both ways. `directorTriggerQueue.test.ts` (2) drives the real chain — `chat.send`, the real router, `startDirector`'s Supervisor and `checkpoints.answer` — with FakeAdapter emitting a real structured turn's shape (no `idle` event). It shows a started Director idle; the first message sent and marked delivered; two messages sent mid-turn held, then sent as one turn after `finished`; and a blocking answer waking it. The module did not exist when the tests were written. **Mutations, each with what changed:** no coalescing → the coalescing, never-merged, checkpoint-alone and heartbeat tests fail (4); a bare-timer heartbeat → "no new events, no turn" fails; mid-turn injection → "nothing is sent mid-generation" and "restart alone" fail (2); no idle after `assign()` → the Director read `starting` and neither case was delivered (2); a task-less turn ending `blocked` → "the next turn" timed out (1). `director.coalesceWindowSeconds` and `reporting.heartbeatMinutes` lose `inactiveUntil`. Suites, one at a time, over S1-14 and S1-15 together: unit 1,039 · integration 997 (153 files, freshly packaged) · `test:security` run 1: 97, run 2: 116 · the four `check:` scripts green |
| S1-16 | A6, B11, `NEXT-VERSION` §H.1 | **Intent classification**: a one-shot call with a keyword fallback | `classifyIntent()` is the **only** place intent is decided. It uses `runOneShot` on the `fast` tier when a provider resolves, and §22.4's keyword and structure rules otherwise. Ambiguity resolves to "chat", and the Director decides inside its turn. Its cost is recorded through X-22's path. It runs on each user-message trigger and feeds S1-14 (new work → `INTAKE`). Tests cover both paths, including provider `none`. Mutation: the fallback removed fails the no-provider test. §H.1 updated | DONE 76b2b13, 2026-09-24. `src/main/director/classifyIntent.ts` is the one decider. It uses `runOneShot` on the `fast` tier when `resolveOneShotConfig` gives a provider, and the cost is recorded by `runOneShot` (X-22). Otherwise it uses §22.4's rules: a work verb plus an artifact is `new_work`; during intake a reply is `answer`; a question word, an auxiliary followed by a subject, or a trailing `?` is `question`; everything else is `chat`. A one-shot reply that is not one of the four words is `chat`, and any failed call falls back to the rules. **Production caller:** the trigger queue's delivery classifies every turn that contains user messages. `awaitingAnswer` is the persisted A.3 state (`INTAKE`). New work in an `IDLE` conversation takes S1-14's `IDLE → INTAKE` (`director.intake_started`), committed before the send, and the turn is told what Bureau read the message as. `main()` passes the pricing table. Tests: `classifyIntent.test.ts` (12). With provider `none`, six phrasings go to all four outcomes, a reply during intake is an answer, and no usage row is written. With a real loopback provider, the call decides and writes one `oneshot` usage row plus one `cost.oneshot_recorded`; a stray reply is `chat`; an unreachable provider falls back to the rules. Plus the rules alone. `directorTriggerQueue.test.ts` gains the chain: "hi, how are you" leaves the state `IDLE`, and "Build me a website for my bakery" moves it to `INTAKE` with one `director.intake_started`. Red first: the module did not exist, and the first draft read "do the thing" as a question (an auxiliary now needs a subject after it). **Mutation** (fallback removed): 7 fail, including the chain's `expected 'IDLE' to be 'INTAKE'`. §H.1 updated. Suites, one at a time: unit 1,039 · integration 1,010 (154 files, freshly packaged) · `test:security` run 1: 97, run 2: 116 |
| S1-17 | A2, B10 (Appendix A.2 slots, attachments), E-3, §26 #4–#6 | **Context assembly (§8.0.1)** with the drop order and a token budget, plus the Director's system prompt | `assembleDirectorContext()` builds the seven layers in §8.0.1's order and drops from the bottom when over `director.contextBudgetTokens`, estimating `chars / 4` plus 10 %. The system prompt is never dropped. `packs/operations/prompts/director.md` gains Appendix A.2's slots (today it is plain prose), and each slot is filled from real rows. Attachments reach the Director per **E-3**. Unit tests cover each drop step and each slot. Mutation: a wrong drop order fails it. `NEXT-VERSION` §L.2 resolved. The setting loses `inactiveUntil` | DONE 4e072a7, 2026-09-24. `src/main/director/assembleDirectorContext.ts`: §8.0.1's seven layers in order. `fitDirectorLayers` drops whole layers from the bottom while the estimate (`ceil(len × 11 / 40)`, i.e. chars / 4 + 10 % in integers) is over `director.contextBudgetTokens`. It never drops the system prompt, and passes over empty layers. `packs/operations/prompts/director.md` is now Appendix A.2's template, with the previous guidance kept. Its slots are filled from real rows: `company_name` (`companies`), `director_state` (S1-14's sentence, which is S1-14's named caller), `standards` (pinned company notes and pinned user notes), `project` (stage, latest brief summary, the latest plan's phases, done/total tasks, spend of budget), `decision_log`, `team` (non-archived employees, role, skills, status), `memory_pack` (retrieved for the latest user message), `recent_conversation` (20 turns, newest first), `max_intake_rounds` and `tool_list` (the Director's handler set). Layers 2, 4 and 6 come from one `composeMemoryPack` call. A dropped layer's slot says it was left out. **E-3:** attachments are named from `payload.attachments` — inside the project "you can Read it", outside "you cannot open it; ask the user". **`user_name` has no source row** (§F), so it reads "the user". **Production caller:** the trigger queue writes the assembly to `director-context.md` in the Director's state directory before every turn, after intent. The claude-code adapter passes it, for the Director only and only once the file exists, as `--append-system-prompt-file` with `--system-prompt-snapshot off`. Measured free on 2.1.276 (no key): the CLI accepts both. Its `--help` says the default snapshot resends the first turn's system prompt on every resume, which would freeze the Director's view. Whether the text reaches the model is for the gate. `main()` passes `baseDir` and `bundledPacksDir`, guarded by `directorWiring.test.ts`. **Tests:** `directorContextBudget.test.ts` (11: the estimate, the order, each drop step, the system prompt never dropped, empty layers passed over); `assembleDirectorContext.test.ts` (3: every slot from real rows with no placeholder left; over budget the conversation goes first and its slot says so; the default budget is the setting); `directorContextFlags.test.ts` (3); `directorTriggerQueue.test.ts` now reads the written file. Red first: the module did not exist, and the first run caught a float (`400 / 4 × 1.1` is 110.000…01, so it rounded up to 111). **Mutation** (drop from the top): `standards` went instead of `conversation`, and 6 fail. `director.contextBudgetTokens` loses `inactiveUntil`. **So does `intake.maxRounds`:** the prompt now reads it, and `settingsNotYetActive` holds that a read setting is not marked inactive. Enforcing the round cap is still S2-2's. `NEXT-VERSION` §L.2 resolved, and `chatPayloads.ts`'s note to M11 records the decision. Suites, one at a time: unit 1,051 · integration 1,015 of 1,016 (156 files, freshly packaged). **One failure, not this row's:** the probe's "hanging binary" test measured 29,999 ms against `>= 30,000` (a 1 ms timer-granularity flake, §F). Rerun alone it passed at 30,007 ms. · `test:security` run 1: 97, run 2: 116 |
| S1-18 | A3, risk #16 | **Compaction** | After `director.compactAfterTurns` turns, or when the estimate nears the limit, a compaction turn writes a structured summary to `conversations.summary`. A **fresh** session is seeded with that summary plus the S1-17 assembly, `director_session_id` is replaced, one `director.context_compacted` is emitted, and the chat gets one plain line saying so. The FakeAdapter test drives the real code. Mutation: the new session not seeded with the summary fails it. Risk #16 updated. The setting loses `inactiveUntil` | DONE 6b1e792, 2026-09-24. Compaction lives in the trigger queue's delivery (`directorTriggers.ts`), which already decides every Director turn. **When:** before a turn is sent, if the Director has completed `director.compactAfterTurns` turns since the last `director.context_compacted`, or if S1-17's assembly had to drop the recent conversation to fit (this code's measurable form of "the estimate nears the limit"). Turns are counted durably from the events, as the Director's own `employee.idle { reason: 'turn_completed' }`, which fires once per turn. `employee.working` fires again after every tool call. **How:** a compaction turn on the old session asks for a structured summary. Its prose is collected by the queue, and the chat producer is kept out of it (`isCompacting()`). A turn that fails or says nothing changes nothing. Otherwise, in one transaction, `conversations.summary` is written and `Supervisor.startFreshSession()` clears the session: the adapter's new `resetSession()` drops `--resume`, and `employees.session_id` is set to null. Then one `director.context_compacted` (reason, turns, summary length, previous session) and one plain system line in the chat. The context is re-assembled before the waiting turn goes out. **Seeded:** A.2's template gains a `conversation_summary` slot in the system prompt, which is never dropped. **`director_session_id` replaced:** when the fresh session reports its id, the queue writes it to `conversations.director_session_id`, as S1-11's deviation said compaction would. `directorCompaction.test.ts` drives the real chain (`chat.send`, router, queue, `startDirector`'s Supervisor with its chat producer) with FakeAdapter emitting real-shaped turns. After two turns the third message waits behind a compaction turn. Then: the summary is stored, the session reset once, the context file carries the summary, one event, one system line, the summary never in the chat, and `sess-new` recorded. Red first: the third message went straight out. Along the way, FakeAdapter now also flushes its queue on `finished`, as the real adapter does on exit, and a race in the test itself was fixed. **Mutation** (the fresh session not seeded): the context file lacked the summary. `director.compactAfterTurns` loses `inactiveUntil`, and `settingsNotYetActive.test.ts` uses a still-inactive key as its example. Risk #16 updated. Suites, one at a time: unit 1,051 · integration 1,017 (157 files, freshly packaged) · `test:security` run 1: 97, run 2: 116 |
| S1-19 | A13, §8.0 | **The budget reserve, and the no-model fallback message.** The reserve carve-out exists (`budgetEnforcement.ts`). The chat's "raise budget" button only logs a console warning (`ChatView.tsx:161`), and nothing acts on a `raise_budget` answer | When the Director itself is `exceeded` (reserve gone), no turn is spawned. The chat gets one `system` `error` message with the `raise_budget` remedy, **written without any model call**. The button opens the real budget control, and answering `raise_budget` on the exhausted-budget checkpoint takes the user there, so neither path is inert. Integration test with a FakeAdapter Director at an exhausted reserve: no turn, and the message is present. Plus a renderer test for the button. Mutation: a spawn attempted at exhaustion fails it | DONE a4e808b, 2026-09-24. **No turn:** `directorBudgetExhausted(db, projectId)` (`budgetEnforcement.ts`) reads the Director's own ceilings without a turn: the FULL project budget and the full daily budget, no carve-out, no per-employee level (§8.0). The trigger queue's delivery checks it before anything else. When exhausted it spawns nothing and returns `deferred`, and the queue puts the batch back unsent, so the messages stay undelivered and go out as one turn once there is money again. **The message:** one `system` `error` message per exhaustion episode (`code: director_budget_exhausted`, the `raise_budget` remedy, the explanation naming which budget), written by `appendChatMessage` with no model call, and pushed through `main()`'s broadcaster. The flag clears when a turn is next spent. **The button and the answer:** the chat's remedy routing moved from an inline switch in `ChatView` to `components/remedies.ts`. `raise_budget` opens Settings at the Budgets group (a new store `openSettings(group)`; the panel outlines, marks and scrolls to it) instead of logging "no destination yet". The shared answering hook calls `afterCheckpointAnswer`, so answering the exhausted-budget checkpoint with `raise_budget` goes there too. **Tests:** `directorBudgetReserve.test.ts` drives the real chain at a spent $1/$1 day. Two messages give no turn, one notice (`system`, `error`, `raise_budget`, no "$0.00") and no new usage row; after the budget is raised, both messages go as one turn. `raiseBudgetRemedy.test.ts` (5): the remedy, the answer (and that `cut_scope` does not), the Budgets group marked with its limits (through a props-driven `SettingsGroups`, since zustand serves its initial state to server rendering), closing forgets the focus, and both production callers in source. Red first: a turn was spawned at exhaustion. **Mutations:** the check skipped → "a Director turn was spawned with the reserve gone" (1 fails); the `raise_budget` route removed → the remedy test fails. Suites, one at a time: unit 1,056 · integration 1,017 of 1,018 (158 files, freshly packaged). **One failure, not this row's:** `fireAndRehire.test.ts`, intermittent, green alone 11/11 (§F). · e2e 28/28 (the renderer changed) · `test:security` run 1: 97, run 2: 116 |
| S1-20 | B7 (§M11 item 7), `NEXT-VERSION` §I.3, §26.1 | **The restart report.** The post-restart grace suppresses expiries and returns `suppressedByGrace`, which nothing reads | At startup with interrupted work (what reconcile repaired, `suppressedByGrace`, pending checkpoints, held messages), **exactly one** restart trigger is enqueued, and it is not coalesced. The Director's turn gets a structured summary and posts one report. With nothing interrupted, no trigger fires (no bare-timer wake). FakeAdapter test covering both cases. Mutation: the grace count dropped from the summary fails it. §I.3 resolved | DONE 4f4f512, 2026-09-24. `src/main/director/restartReport.ts`: `buildRestartSummary` reads, at startup, the repairs `reconcile()` made to work in flight (orphans stopped, tasks blocked, replies cut off, commits resolved, leases reclaimed, parked employees resumed, stale permission requests cancelled), the grace's count, the pending checkpoints and the held (`pending`) outbox messages. It returns null when there are none. Bookkeeping that every restart does (stale `control.json`, counters, mirror rows) is deliberately left out, because counting it would buy a Director turn on every launch. The grace count comes from a new `countSuppressedByGrace` in `checkpointsTick.ts`, which `resolveExpiredCheckpoints` now uses too: one derivation, and no risk of resolving anything. `renderRestartSummary` gives the turn fixed sections and tells it to post ONE `bureau_report`. `offerRestartReport` offers a `restart` trigger keyed on the start instant (so exactly one), which the S1-15 queue sends alone. **Production caller:** `main()` keeps `reconcile()`'s result (`const reconciled`) and, once the checkpoints tick is up, builds the summary and offers it (`directorWiring.test.ts` guards both). This is the restart producer S1-15 named. `restartReport.test.ts` (2) runs a real `reconcile()` over a cut-off reply, an expired checkpoint and a held message. One turn, from two offers, carries each fact, including "1 decision past its deadline was held back". A clean start gives null and no turn. Red first: the module did not exist. **Mutation** (the grace count dropped): the summary no longer names the held decision, and the case fails. §I.3 resolved. Suites, one at a time: unit 1,057 · integration 1,020 (159 files, freshly packaged) · `test:security` run 1: 97, run 2: 116 |
| S1-21 | CI runs 35881694041 and 35900273899; reproduced on the dev box 2026-09-24 | **Every PowerShell Bureau spawns inherits the parent's `PSModulePath`, and a PowerShell 7 parent breaks it.** PowerShell 7 puts its own module folders first on `PSModulePath`; a child `powershell.exe` (5.1) inherits that, resolves a built-in module to PS7's Core-only copy, and cannot load it: `Get-Acl : The 'Get-Acl' command was found in the module 'Microsoft.PowerShell.Security', but the module could not be loaded` (`CouldNotAutoloadMatchingModule`). GitHub Actions runs every step in PowerShell 7, which is why CI is red; a user who starts Bureau from a PowerShell 7 terminal (VS Code with pwsh as its shell, for one) gets the same. **Two sites, one defect:** (1) `tokens.ts`'s descriptor read — `control.json` never verifies, is deleted, and no employee can start (all 82 failures in 35900273899, including S1-12b's ten Director-tool cases, are this one cascade); (2) `process/processInfo.ts`'s `getProcessStartTime` — `Get-Process` is shadowed the same way, the failure is caught and returned as `null`, which its contract defines as “already dead”, so `reconcile.ts`'s `sweepOrphans` treats a **live** orphan as dead: never killed, no `employee.orphan_killed`, no secret revocation, silently. It also calls bare `powershell.exe` through PATH. S1-9's Job Object now covers the common crash case, so this is the second line of defence, not the only one. **Reproduction (verified 2026-09-24, needs no PowerShell 7):** a folder holding `Microsoft.PowerShell.Security\Microsoft.PowerShell.Security.psd1` with `CompatiblePSEditions = @('Core')`, `PowerShellVersion = '7.0'`, `CmdletsToExport = @('Get-Acl')`, `NestedModules = @('Microsoft.PowerShell.Security.dll')`, placed first on the parent's `PSModulePath`, gives the runner's exact message; the same shape for `Microsoft.PowerShell.Management` / `Get-Process` makes a live PID read as dead | **One helper, both sites** (standing rule 6): a single function spawns Windows PowerShell by absolute path (`%SystemRoot%\System32\WindowsPowerShell\v1.0\powershell.exe`, `-NoProfile -NonInteractive`) with `PSModulePath` set explicitly to `%SystemRoot%\System32\WindowsPowerShell\v1.0\Modules` and the rest of the environment inherited. `tokens.ts` and `processInfo.ts` both call it; a test lists every `powershell` spawn in `src/` and fails on one that bypasses it. `tokens.ts`'s `icacls` calls move to its absolute path too (same file, same rule as `whoami`). **`getProcessStartTime` stops conflating “dead” with “could not tell”:** process not found stays `null`; a failed read is a distinct result. `sweepOrphans` never kills a PID it could not verify — killing a reused PID is irreversible — and the refusal is **not silent**: it is recorded as an activity event (a new type needs its §0.1 row and `check:event-taxonomy`, as S1-12b did for invariant #5). **Failing tests first, both reproduced on the dev box** with the recipe above in the parent's environment: the real `writeControlJsonWithAcl` verifies, and `getProcessStartTime` reads a live process. Both fail before the fix for the runner's reason. Mutations: the helper not setting `PSModulePath` (both fail); `processInfo` bypassing the helper (the listing test fails); the unverified branch killing (fails). The S1-21 attempt-2 improvements (`Get-Acl`, the named parse failures, the descriptor in the error) stay. **Done when CI is green on a push**, not when the dev box is. If that push is red, stop and report the log line — no fourth fix without it | DONE f66b3b2, closed on CI run 36003599459 (2026-09-24): every step green on the runner — unit, package, integration, both security invocations, contract and e2e. **attempt 4, the runner's own user.** Fixed 2026-09-24; hash recorded when CI reports, for the same reason as attempt 3. (a) The reader is `(Get-Acl).GetAccessRules($true, $true, [SecurityIdentifier])` through the S1-21 helper, one `Allow|Deny <SID>` line per ACE; `SDDL_SID_ALIASES` and `daclTrusteeSids` are gone (`aclTrusteeSids` parses the lines, and an empty read or any other line is refused with what was read quoted). Comparison unchanged. (b) `restrictFileToCurrentUser` (`tokens.ts`, exported) is the one narrowing: `/reset`, then `/inheritance:r` with the two grants, by `icacls`'s absolute path; `writeControlJsonWithAcl` and `realEngineKey.test.ts`'s `restrictedKeyFile` both call it, and the key helper's hint names `/reset` first. (c) **Red first, for the runner's reason, byte for byte:** a temp file granted `*S-1-5-18:(F)` and the built-in Administrator (`…-500`), with `currentUserSid` injected as that SID, was refused with `cannot resolve to a SID: LA (descriptor: D:PAI(A;;FA;;;SY)(A;;0x12019f;;;LA))`; after the fix it reads back as `Allow S-1-5-18` / `Allow …-500` and verifies. The real-file no-alias test was red on `D:PAI(A;;FA;;;SY)…` and is green on `Allow S-1-5-18`. **Mutations, with the value that changed:** reader back on SDDL → the LA file reads `D:PAI(A;;FA;;;SY)(A;;0x12019f;;;LA)` again, both new tests red; narrowing without `/reset` → the explicit-Administrators test is refused with `[read: Allow S-1-5-18 Allow S-1-5-32-544 Allow …-1002]`. The SDDL-shaped injected fixtures became listings; the alias-refusal case became "a line that is not `Allow|Deny <SID>` is refused, quoted" (`Allow LA`). Suites, one at a time: unit 1,030 · integration 986 (149 files, freshly packaged; the staleness gate fired by name on a touched `src/` file, mtime restored) · `test:security` run 1: 97, run 2: 116. Not pushed. **Attempt 3, the cause.** Fixed 2026-09-24; hash recorded when CI reports, because this row's own rule is "done when CI is green on a push", not when the dev box is. **One helper** (`src/main/process/windowsPowerShell.ts`): absolute `powershell.exe`, `-NoProfile -NonInteractive`, `PSModulePath` set to `%SystemRoot%\System32\WindowsPowerShell\v1.0\Modules`, rest of the environment inherited, `env` injectable. `tokens.ts` (descriptor read) and `processInfo.ts` both call it; `tokens.ts`'s `icacls` and `whoami` now resolve through the helper's `windowsSystem32()`. `powerShellSpawnsGoThroughOneHelper.test.ts` lists every `powershell` mention in `src/` outside comments and fails on one that is not the helper. **`getProcessStartTime` returns three answers**, not `string | null`: `alive` / `not_found` / `unreadable`, told apart by a word PowerShell prints rather than by an exit code — measured on the dev box, the old script threw *identically* for a freshly-killed process and for a shadowed `Get-Process`, which is the conflation itself. `sweepOrphans` never kills an `unreadable` PID and emits `employee.orphan_unverified` (new type: §5.2, §4.4 and a §0.1 amendment row; `check:event-taxonomy` green at 143 types). **Reproduced in the suite, no PowerShell 7 needed** (`tests/helpers/shadowedPowerShellModules.ts`): Core-only manifests first on the parent's `PSModulePath`, in a temp dir the test removes, no `process.env` mutated. All three failing tests confirmed red first, for the runner's reason — `Get-Acl`'s exact "the module could not be loaded", and the live-PID-reads-as-dead symptom measured against the pre-fix code (a demonstrably live PID returned `null`, which the old contract defined as "already dead"). **Mutations, each with the value that changed:** the helper inheriting `PSModulePath` instead of setting it (3 red across both sites); `processInfo.ts` spawning `powershell.exe` itself (listing test red, 2 cases); the unverified branch killing (red). **Found while fixing, and fixed here because it defeats the fix:** `{ ...env, PSModulePath: x }` is **not** a replacement on Windows — env var names are case-insensitive, object keys are not, and Vitest's `process.env` snapshot carries `PSMODULEPATH`, so the child kept the old value and the fix was silently inert. The helper strips every case variant. §F carries the general hazard. **Attempt 1** (S1-1): `icacls /save` — red on the runner, cause hidden because the error dropped the descriptor. **Attempt 2** (`d9d4ebd`): `Get-Acl`, and the error now carries what was read — red again, and that evidence named the real cause in one line (35900273899). **Diagnosed 2026-09-24** in review: the inherited PowerShell 7 `PSModulePath`, reproduced locally, and found to affect `processInfo.ts` too **Attempt 3** (`c8a970e`, CI run 35981947951): the PowerShell fix **worked** — the runner now reads the descriptor — and the evidence it printed names the last cause. The runner's account `runneradmin` is the machine's **built-in Administrator (RID 500)**, which SDDL writes as the alias `LA`: `D:PAI(A;;FA;;;SY)(A;;0x12019f;;;LA)` is a **correct** ACL (SYSTEM + the user) that the check refuses because attempt 2 deliberately treats `LA` as unresolvable. All 37 failures in 8 files are that one cascade. **Reproduced exactly on the dev box 2026-09-24:** a temp file granted `*S-1-5-18:(F)` and `*<machine SID>-500:(R,W)` (the built-in Administrator exists on every Windows machine; its SID is the current user's with the last RID replaced by 500) prints the runner's descriptor byte for byte. A second cause sits behind it: `realEngineKey.test.ts`'s `restrictedKeyFile` runs its own `icacls /inheritance:r /grant:r` with no `/reset`, so on the elevated runner an explicit `BA` ACE survives (`D:PAI(A;;FA;;;SY)(A;;FA;;;BA)(A;;FR;;;LA)`) — the trap `tokens.ts` already documents, re-implemented by a test (standing rule 1); the helper's own “restrict it with” hint omits `/reset` too. **Attempt 4's Done-when:** (a) trustees come from `(Get-Acl -LiteralPath <file>).GetAccessRules($true, $true, [System.Security.Principal.SecurityIdentifier])` through the S1-21 PowerShell helper, one `Allow|Deny <SID>` line per ACE, so every trustee is a raw SID and **no alias table exists** (verified on the dev box: the `LA` file reads back as `S-1-5-18` and `…-500`); `SDDL_SID_ALIASES` and the alias branch go. Any line that is not that shape is refused with the output quoted (invariant #6), and the evidence still travels in the error. The comparison is unchanged: any ACE naming a forbidden SID fails, and the current user's SID must be present. (b) The ACL-narrowing step is **one exported function** (`/reset`, then `/inheritance:r` with the two grants, by `icacls`'s absolute path) that `writeControlJsonWithAcl` and every test fixture restricting a file call; no test runs its own narrowing `icacls`. The helper's hint names `/reset` first. (c) **Failing test first, the runner reproduced locally:** the `LA` file above, with `currentUserSid` injected as the `…-500` SID, is refused before the fix (`cannot resolve … LA`) and verifies after; a real-file test asserts the reader never emits an alias (`SY` comes back as `S-1-5-18`). Mutations: the reader back on SDDL (the `LA` test fails); the shared narrowing without `/reset` (the existing explicit-Administrators test fails). **Done when CI is green on a push.** If red again, stop and report the log line |
| S1-22 | Review of §S1's output 2026-09-25; §F S1-17 (probe timer), §F S1-19 (`fireAndRehire`) | **Two kinds of flaky test, both able to turn CI red for no reason.** (1) **Name-pool collisions in fixtures:** a test hires someone with a *random* name from `EMPLOYEE_NAME_POOL` (56 names), then hires a *fixed* name that is also in the pool. When the dice match, Bureau correctly refuses (`FirstNameTakenError`) or correctly rehires the archived namesake — the product is right, the fixture is nondeterministic. Seen twice: `assembleDirectorContext.test.ts` failed in the review's packaged run (`"ravi" is already this company's first name`, the Director having drawn Ravi), and §F S1-19's `fireAndRehire` "names someone new" failure is the same shape with Nadia — **not** an identity bug. A rough scan finds fixed pool names (Ravi, Priya, Mei, Nadia) in about a dozen test files. (2) `claudeCodeAdapterProbe.test.ts`'s hanging-binary case asserts `>= 30,000` ms against a 30 s timer and measured 29,999 once (§F S1-17) | **No test fixture can collide by chance.** Every fixed name a test hires is outside `EMPLOYEE_NAME_POOL`, or every pool allocation in tests is deterministic — pick one mechanism, not both, and say which. A guard test fails if a test under `tests/` hires a fixed name that is in the pool (or, if the deterministic route is chosen, if a hire in `tests/` can reach the random allocator). The probe assertion gets a tolerance of a few ms, or a monotonic clock on both sides. **Failing test first:** force the collision (seed or stub the draw so the Director draws Ravi) and show `assembleDirectorContext`'s setup throw before the fix. Standing rule 9: print the drawn name. The two §F lines are marked moved to this row. Product code in `allocateName.ts` and `hireEmployee.ts` does not change: refusing a duplicate first name is §6.8 | DONE 47c1f28: **mechanism: every fixed name in `tests/` is outside `EMPLOYEE_NAME_POOL`** — the allocator is untouched, so no test can reach the random draw with a name it could collide with. Ravi→Quinn, Priya→Wren, Mei→Ulla, Nadia→Vera (none in the pool), lowercase and derived paths included, in 66 files. Guard: `tests/unit/testFixtureNamesAvoidThePool.test.ts` fails on any string literal in `tests/` whose first word, case-folded, is a pool first name (every literal, not only hire arguments, because a fixture's name travels into raw `INSERT`s, adapter contexts and e2e assertions). **Failing first:** red on 46 files; a throwaway copy of `assembleDirectorContext.test.ts` with `allocateName` stubbed to draw Ravi printed `DRAWN NAME: Ravi (real draw would be Mateo / Eitan / Mei)` and all 3 setups threw `"ravi" is already this company's first name`; after the rename the same stubbed draw passes 3/3. Mutation: one fixture back to `'Ravi'` fails the guard naming that file. Probe: the lower bound is `ceiling − 5ms`, with the reason in a comment — the timer fires on the event loop's cached clock, so a monotonic clock would not close the gap. Product code unchanged. Suites, one at a time: unit 98 files / 1059; `npm run package`, integration 159 / 1020, staleness gate fired by name on a touched `src/` file (mtime restored); contract 31 (+7 opt-in skipped); e2e 28; `test:security` run 1: 97, run 2: 116. No real runs |

---

## §S2: Intake → brief → plan (session 2)

| ID | Source | What (plain) | Done when | Status |
|---|---|---|---|---|
| S2-0 | §F S1-13 (review of §S1, 2026-09-25), invariant #1 | **A chat message a tool handler posts is saved but never pushed to the open window.** `bureau_report` calls `appendChatMessage` with no broadcaster, because a `ToolHandler`'s context has none, so the card appears only after the next re-hydrate. The Director's streamed prose is pushed (it goes through `main()`'s `ChatStreamRegistry`); its cards are not. S2-3's brief card is the first card the user must **see to act on** — an unseen approval card stalls the whole conversation, which is invariant #1 | **Every chat message a Core-side path posts reaches an open window,** through the one broadcaster `main()` already builds: the control channel's handler context carries it, and the tool handlers post through it rather than calling `appendChatMessage` bare. A test lists every `appendChatMessage` call in `src/` and fails on one that bypasses the broadcaster (or documents why it may). **Failing test first:** over the real control channel, `bureau_report` posts a card and an attached renderer-side listener receives it **without** a re-hydrate — red before the fix. Mutation: the handler back on bare `appendChatMessage` fails it. Lands before S2-3 | DONE b499fac: `ToolHandlerContext` and `ControlChannelServerOptions` gain an optional `chatBroadcaster`; the server hands it to every handler, and `bureau_report` posts through it. `main()` now builds its one `createElectronChatBroadcaster()` before the control channel (it reads the window list at send time, so that is safe) and gives the same instance to the server and to `reportDirectorStart`, the other writer that bypassed it (its deps are now `ChatDeps`). **Failing first:** `tests/integration/director/directorCardsReachTheWindow.test.ts` — the real Director started on FakeAdapter, `bureau_report` over the real control channel, a listener on the broadcaster the server was given, no `chat.listMessages`: the call succeeded and the listener received `[]`. `tests/unit/chatWritesReachTheWindow.test.ts` lists every `appendChatMessage` call in `src/` (7 today; it fails if it finds fewer) by its first argument and failed naming exactly `report.ts` and `startDirector.ts`; `chatDeps(ctx)` in `ipc/handlers/chat.ts` is the one documented builder that carries it. A third case checks `main()`: one broadcaster, handed to `new ControlChannelServer` and to `reportDirectorStart`. Mutations: `report.ts` back on bare deps (listener `[]`, and the guard names the file); the server not putting it in the handler context (listener `[]`); `main()` not passing it to the server (wiring case fails). Suites, one at a time: format, lint, typecheck, the four `check:` scripts; unit 99 files / 1062; `npm run package`, integration 160 / 1021, staleness gate fired by name on a touched `src/` file (mtime restored); contract 31 (+7 opt-in skipped); e2e 28; `test:security` run 1: 97, run 2: 116. No real runs |
| S2-1a | A7 (part), B12 (§M11 item 12), `NEXT-VERSION` §K.2, `projects.create`/`open` stubs, Nikunj's S2-1 decision (2026-09-25) | **Every Director turn carries its conversation.** Today "the conversation the Director is in" is `resolveConversationForDelivery(db, null)` — the most recently created — in the trigger queue's intent step, context assembly, the reserve's notice, `bureau_report` and `resolveDirectorProject` (`${project}`, `bureau_get_project_state`, `bureau_search_workspace`). The Director also runs **one** engine session, so a resumed session would carry one project's transcript into another's turn | Each trigger names its conversation, and a turn is built from one conversation's triggers only (never coalesced across conversations). The turn's conversation is the one every step above uses, through one function. The engine session is per conversation: `conversations.director_session_id` is resumed for that conversation's turn, or a fresh session started and recorded. A tool call outside any turn falls back to the company conversation, so a project-scoped tool refuses (invariant #6). **Isolation test with two deliberately similar projects (two restaurants):** a message in one project's conversation assembles only that project's brief, plan and memory, is answered in that conversation, resumes that conversation's session, and writes nothing to the other's rows. Mutation: the turn falling back to "most recent" fails it | DONE c41e8d4: **one decision, one place:** `src/main/director/directorConversation.ts`. Every `DirectorTrigger` carries a required `conversationId` (`null` = the company conversation, for the restart report and the heartbeat); outbox messages resolve theirs in `conversationOfOutboxMessage` (`chat.send` now puts its conversation in `thread_id`; an answer goes by its checkpoint's project, an employee's message by its task's). The queue never batches across conversations (the oldest waiting at the winning priority decides). `deliverTurn` uses the turn's conversation for the reserve check and notice, intent, context and compaction, and hands it to `Supervisor.deliverDirectorTurn`, which holds it until the turn ends (`directorTurnConversationId`). The chat producer, `bureau_report` and `resolveDirectorProject` (so `${project}`, `bureau_get_project_state`, `bureau_search_workspace`, `bureau_write_memory`) read it back through `resolveDirectorConversation`; between turns that is the company conversation, which has no project, so those refuse. **Session per conversation:** `Supervisor.switchDirectorSession` resumes the conversation's `director_session_id` (or starts fresh) before the turn, and every `session.started` is recorded on the conversation it ran for. §8.0's "one persistent session per company" amended, with a §0.1 row. `resolveConversationForDelivery`'s fallback is now the company conversation, not "the most recent" (the router and the checkpoint surfacer use the same rule). **Failing first:** `tests/integration/director/directorConversationIsolation.test.ts` — two restaurants (a trattoria and a pizzeria) with their own brief, plan, memory and session; a message in the trattoria's conversation through `chat.send` → router → queue → real Director on FakeAdapter. Red: the context was the newest conversation's (no trattoria brief) and no session was resumed. Green: only the trattoria's brief, phase and memory are assembled, `session-trattoria` is resumed, the `bureau_report` card and the prose land in its conversation, its new session id is recorded there, and the pizzeria's row is byte-for-byte unchanged; then the reverse. **Found by it:** `composeMemoryPack`'s search took any project's notes (no project filter on the `project` scope), so the pizzeria's decision reached the trattoria's pack — employees' packs share the path. Fixed here with `searchMemory`'s `projectScopeRef` because the row's Done-when cannot hold without it (§F). Mutations, each printed and each caught: the turn back on "most recent" (resumed `session-pizzeria` for the trattoria), no session switch (resumed nothing), `bureau_report` without the turn (card in the company conversation), memory search unscoped (pizzeria memory in the trattoria's context). Queue unit tests: two conversations' user messages are two turns, and coalescing never merges conversations. Tests that call project-scoped tools now say they are in a turn (`tests/helpers/directorTurn.ts`), and a new policy case denies `Read(${project}/**)` between turns. Suites, one at a time: format, lint, typecheck, the four `check:` scripts; unit 99 files / 1064; `npm run package`, integration 161 / 1023, staleness gate fired by name (mtime restored); contract 31 (+7 opt-in skipped); e2e 28; `test:security` run 1: 97, run 2: 116. No real runs |
| S2-1b | A7 (part), B12 (§M11 item 12), `NEXT-VERSION` §K.2, `projects.create`/`open` stubs, Nikunj's S2-1 decision (2026-09-25) | **A project created from chat.** Nothing in production calls `insertProject`, and §5.1 says a new project binds the **current** conversation (decision: bind it, §F S2-1) | When intent is new work **in the company conversation**, one transaction creates the project (stage `intake`), binds the current conversation to it, and creates a fresh company-level conversation, with `project.created` and `director.intake_started` (§5.1: there is always one company-level conversation). **In a project's conversation**, new work does not create anything: the turn tells the Director to offer it as a new project. `bureau_set_project_stage` is built and validated against §8's transitions and A.3, and its `intake` is the same one creation function (from a project's conversation it creates the new project with its own conversation — the accepted offer). `projects.create` and `projects.open` stop being stubs. Handler-level tests | DONE (hash in the next commit): **one creation function**, `src/main/projects/createProject.ts`, with three callers: the intent step, `bureau_set_project_stage`'s `intake`, and `projects.create`. One transaction: the project at `intake` (name from the request in plain code — "Build me a website for my bakery" → "A website for my bakery"; path `<home>/<slug>`, not created on disk; kind `mixed` until the brief says, §F), the conversation **bound** (§5.1) or a **new** one, a company conversation if none is left, and A.3's `IDLE → INTAKE` validated by the one table (`writeDirectorTransition`, split out of `transitionDirectorState` because `logEvent` refuses to run inside a transaction). Then `project.created` and `director.intake_started`, once each. **Intent step:** new work in the company conversation creates and binds; in a project's conversation nothing is created and the turn tells the Director to offer a new project (it had been moving that conversation into INTAKE). **`bureau_set_project_stage`** (registered in `DIRECTOR_TOOL_HANDLERS` and `bureau-tools`; the Director now has 8 of its 19): `intake` from the company conversation binds it; from a project's conversation it needs `name` (§7.9 amended, §0.1 row) and gives the new project its own conversation, leaving the current one untouched. Other stages go through §8's table (`src/main/projects/projectStage.ts`): the Director's three moves each carry their A.3 partner and are written together or not at all; the user's four (brief → planning, planning → executing, review → executing, review → delivered) are refused with who makes them (invariant #2); anything else is refused as not a §8 move. **`projects.create`** pre-creates with a conversation of its own (absolute path required); **`projects.open`** returns the project's conversation id (its output schema changed from `{ ok }`, for S2-1c's switcher). **Failing first:** `tests/integration/director/projectFromChat.test.ts` through `chat.send` → router → queue → real Director: red with no project created, and with a project's conversation wrongly moved into INTAKE. Ten cases, handler-level through the real control channel in a turn: bind + fresh company conversation + one event each; offer-not-create inside a project; atomicity (a refused A.3 step after the insert leaves no project, no binding, no conversation, no event); the tool's intake both ways; a Director move with its partner; the user's move and a non-move refused; a partner invalid from the current state refused with neither half written; no project, no move; `projects.create`/`open`, and a relative path refused. Mutations, each caught: the chat path not binding, no fresh company conversation, the Director allowed the user's moves (it then errors instead of refusing), creation outside a transaction. Suites, one at a time: format, lint, typecheck, the four `check:` scripts; unit 99 files / 1064; `npm run package`, integration 162 / 1033, staleness gate fired by name (mtime restored); contract 31 (+7 opt-in skipped); e2e 28; `test:security` run 1: 97, run 2: 116. No real runs |
| S2-1c | A7 (part), B12 (§M11 item 12), `NEXT-VERSION` §K.2, `projects.create`/`open` stubs, Nikunj's S2-1 decision (2026-09-25) | **The conversation switcher** (§K.2) | **A list, not tabs**: the company conversation plus one entry per project, each with the project's status and an unread or waiting marker, computed in the Core (`chat.listConversations`), not the renderer (invariant #11). Shown once there is more than one conversation. Renderer tests, and an e2e that switches between two conversations. §K.2 resolved | OPEN |
| S2-2 | A7, B13 (§M11 item 13), invariant #9, risks #7 and #8, E-6 | **Intake:** batched questions, a round cap, "you decide", assumptions, **the deliverable-shape recommendation**, and invariant #9's check before asking | Questions go out through the shape in **E-6**, and its handler enforces the rules in plain code. It refuses fewer than 2 or more than 4 questions. It counts rounds in `director_state_data`. Past `intake.maxRounds`, it refuses another round and tells the Director to write the brief with assumptions. Before posting, **each question is checked against the decision log, the brief and memory** (the existing FTS and similarity path), and a match is refused with the earlier answer returned. `bureau_record_decision` is built on `appendDecisionLog`. The prompt carries "you decide" (decide, state the consequence, move on) and **the deliverable-shape rule: recommend local vs hosted with consequences, never ask**. Tests: each refusal, and a scripted "you decide" leading to a recorded decision. Risks #7 and #8 rows updated with the tests. The setting loses `inactiveUntil` | OPEN |
| S2-3 | A8, B10 (`requestEdit`), `NEXT-VERSION` §L.4, §8.5.2, invariant #2 | **`bureau_write_brief` → card → approval → `deliverables` rows**, and `requestEdit`'s event | The tool validates §8.3's `Brief`, inserts a `briefs` row (`awaiting_approval`), posts the `brief` card, emits `project.brief_drafted`, and moves the state to `AWAITING_BRIEF_APPROVAL`. `brief.approve` creates one `deliverables` row per `Brief.deliverables[]` (`draft`) **in the same transaction**, then enqueues the Director (→ `PLANNING`). **`isBriefApproved(projectId)` is the one function** every later step asks. §5.2 gains `project.brief_changes_requested` and `project.plan_changes_requested`, with a `§0.1` row. `brief.requestEdit` and `plan.requestEdit` become real: one event each, and the feedback reaches the Director as a trigger. Tests cover each step. Mutation: deliverables created outside the approve transaction fails the kill-point test. §L.4 resolved | OPEN |
| S2-4 | A9, risk #9, §8.4 | **`bureau_write_plan`:** phases, tasks and deps in one transaction, with validation | The tool refuses unless `isBriefApproved`. It validates: non-empty `acceptance_criteria`, no dependency cycle, a valid `phase_index`, known `required_skills`, and more than 15 tasks in a phase rejected (§8.4: "the phase is really two"). It inserts `plans`, `phases`, `tasks` and `task_deps` in **one** transaction and emits `project.plan_drafted`. The plan card shows cost per phase and in total. `plan.approve` moves the state to `SUPERVISING` and enqueues the assignment loop (the caller arrives in S3-2). Tests: each rejection, and a kill point proving no partial plan. Mutation: the cycle check removed fails it. Risk #9 updated | OPEN |
| S2-5 | B9 (§M11 item 9), `NEXT-VERSION` §N.5 | **Bound `buildFullSnapshot`'s tasks slice** before plans create tasks | The tasks slice is scoped to the active project, and `liveState` reads projects and tasks through their own readers instead of rebuilding the whole snapshot. A test with 2 projects × 300 tasks: the push carries only the active project's tasks, and a `task.*` burst does not rebuild the other slices. §N.5 resolved | OPEN |
| S2-6 | B14 (§M11 item 14), `NEXT-VERSION` §J.5, §9.3 | **§9.3's grouped checkpoint message.** Batching decides the groups today, but nothing writes the one message "grouped by the Director" | A settled batch from `groupPendingCheckpoints` becomes **one** coalesced Director trigger. The Director answers what it can from the brief and memory, then posts **one** grouped message referencing the rest. If no Director turn is possible (reserve exhausted), a plain-code grouped card is posted instead, so nothing sits unsurfaced. Test: three non-blocking checkpoints inside the window produce exactly one message that references all three. §J.5 updated | OPEN |
| S2-7 | B4 (§M11 item 4), `NEXT-VERSION` §L.1, §28 M9 gate | **§28 M9's deferred gate:** a full conversation including brief approval, with a real producer | An e2e test in the packaged app, **with nothing seeded**. The user types a request, and the **real Director code** runs with FakeAdapter supplying scripted engine output that calls Bureau tools over the real control channel, through S1-11a's bridge. The Director asks one batch of questions, and the user answers. A brief card appears, the user clicks Approve, and `deliverables` rows exist. §L.1 closed. Per §L.1: no brief is inserted by the test | OPEN |

---

## §S3: Work → review → deliverable (session 3)

| ID | Source | What (plain) | Done when | Status |
|---|---|---|---|---|
| S3-1 | Already decided (EmployeeContext), C (§F P-2), `NEXT-VERSION` §H.6, §M.5, B10 (Appendix B) | **Composing an `EmployeeContext` from a hired employee, and the Appendix B prompt** | `composeEmployeeContext(db, employeeId, taskId)` is a production function; `m7ToM4Boundary.test.ts` is a shape, not an import. It does **not** resolve a model: a test lists the `resolveModelTier` call sites, and a new one fails it. The employee's first message renders every Appendix B slot: role prompt, task, acceptance criteria, brief summary, `decision_log` and `memory_pack` (one composition), worktree path, autonomy, and `escalate_when`. A generic-pty employee's adapter is built with `boundCommand` from the role's `engine_options.command`. Unit tests per slot. §H.6 and §M.5 resolved | OPEN |
| S3-2 | A10, B2, B3 (§E-2 lease), C (§F P-5), §26.2, §10.6 rules 1–2 | **The autonomous assignment loop**, in plain code | `eligibleEmployees()` is the one eligibility function, and the loop and `bureau_assign_task` both call it (the tool refuses with a reason). The loop computes the ready set, filters by §8.5, and picks by the deterministic key. **No Director turn per assignment** (the test counts turns). **§10.3's guarantee:** assignment runs in one transaction, a concurrent second assignment gets a typed refusal, it is restart-safe with reconcile releasing, and a two-concurrent-assignments test proves it. An employee is spawned through the production chain from S1-8, S1-9 and S3-1. An `off` employee is started. `orchestrator.maxConcurrentEmployees` is respected. A phase starts by creating `bureau/phase/<n>` (rule 1), and a task branches from it (rule 2). **The worktree is checked to exist before each task**, and a missing one is recreated or the task is blocked with a reason (tested). `bureau_assign_task` built. The settings lose `inactiveUntil` | OPEN |
| S3-3 | B8 (§M11 item 8), `NEXT-VERSION` §J.3, §8.5 | **The unfillable role, and hire proposals** | A message held for `no_idle_employee_for_role`, and a ready task with nobody eligible, each enqueue a coalesced Director trigger. `bureau_hire_proposal` raises a `decision` checkpoint that states the cost. Accepting it hires through the real `hireEmployee`. When no hire is possible, the task waits with a recorded reason and the Director tells the user why. Tests for each branch. §J.3 resolved | OPEN |
| S3-4 | A11, risk #10, C (§F risk #10), §8.5.1 | **Task-completion evaluation.** `bureau_task_done` does not call `commitTaskWork` today | `bureau_task_done` → the Core commits and runs validators. A validator failure blocks the task and gives one repair attempt. On a pass, a coalesced Director trigger goes out with the diff, the summary and the validator output (`bureau_get_task_detail`). `bureau_accept_task` merges into **the phase integration branch, never `base_ref`**: the test asserts `base_ref` is unchanged (§F P-6). `bureau_reject_task` creates a follow-up task in the same phase. When the Director cannot tell, a `reviewer` role or a `review` checkpoint takes it. `review.autoAcceptTrivialTasks` is wired. **Risk #10:** an employee reports done while the validator's tests fail, and the task is not accepted. Mutation: the merge target set to `base_ref` fails it | OPEN |
| S3-5 | B1 (§M11 item 1), C (§F P-6), A12 (phase review), §10.6 rule 5, `NEXT-VERSION` §D.2, `phases.*`/`deliverables.*` stubs | **Phase review, and rule 5: merge only on phase acceptance** | When a phase's last task is done, a phase-review trigger fires (not coalesced). `bureau_request_review` moves the phase to `review`, moves its deliverables to `in_review`, and posts the card with what was verified and what was **not**. `phases.accept` makes the Core merge `bureau/phase/<n>` into `base_ref`, the only write to it. **If `base_ref` is checked out in the user's own checkout, the ref is never moved under it:** the merge updates that checkout only when it is clean and the merge is a fast-forward, and otherwise raises a blocker checkpoint that says why. Both cases tested. `phases.requestChanges` turns feedback into tasks in the current phase. `deliverables.accept`, `reject` and `openFolder` become real. The next phase starts. `phase.*` and `deliverable.*` events. §D.2's rule-5 half resolved | OPEN |
| S3-6 | A12, §8.5, §8.8 | **Reports, re-planning, stalls, stops** | A progress report at each phase boundary, plus the heartbeat's content. `bureau_amend_plan` raises a `decision` checkpoint when cost or scope changes and applies silently otherwise. A stall past `orchestrator.stallTimeoutS` enqueues a trigger. `orchestrator.maxReassignments` failures raise §8.8's blocker. `bureau_stop_employee` parks. Tests for each. The settings lose `inactiveUntil` | OPEN |
| S3-7 | A15, E-4 | **§12.4's "at most once per phase"**, now that phases exist | Per **E-4**. §12.4's sentence and the code say the same thing, tested with two proposals on either side of an answered review | OPEN |
| S3-8 | C (§F X-9) | **Six Core checkpoint kinds state no reversibility**, so they never expire and drain only when the user acts: budget exhausted, circuit breaker, supervisor budget park, dead letter, push detected, and integration merge | While one is pending, it appears in the restart report (S1-20) and in every heartbeat report, and it is never auto-resolved. One test per kind | OPEN |
| S3-9 | `NEXT-VERSION` §D.2 (rule 6), E-5 | **§10.6 rule 6: a push initiated by the Core** | Per **E-5**. §10.6 and §D.2 record the result | OPEN |
| S3-10 | B5 (§M11 item 5) | **A guard ledger for all 19 Director tools** | A table in this row: each tool, any argument that is a path, ref, branch or URL, and the handler-level test that guards it. Every such argument has a test, and none is left to policy (invariant #5) | OPEN |
| S3-11 | A14, F (§19), risks #7 and #8 | **Director behaviour tests** (§19), as scripted conversations against FakeAdapter, driving the real Director code, with tool calls through S1-11a's bridge | Each property is proven as the mechanism the code enforces. **Never builds before approval:** `bureau_write_plan` is refused, and no employee is spawned or assigned before `isBriefApproved`. **Batches:** a one-question round is refused. **Never re-asks:** a question the decision log already answers is refused. **Escalates on ambiguity:** an ambiguous intent reaches the Director as chat, and an approach that has failed twice raises a blocker. **Reports what was not verified:** accept and review refuse an empty `not_verified` field, and the card renders it. The file states in its header that the model's *judgement* is tested by the gate, not here. Mutations: one per property | OPEN |

---

## §GATE: on something real

**Describe a real project in chat → interviewed → brief approved → plan
approved → employees work → phase review → a deliverable exists.** On the real
engine, with the API key, budgets per E-7, and a deliberately small project
(one phase, 3–5 tasks). FakeAdapter proves mechanisms; this proves the product.

| Record | Value |
|---|---|
| The project (what was asked, in the user's words) | |
| Key moments of the transcript (the question batch, the recommendation, the brief's assumptions, the plan, a completion evaluation, the phase review) | |
| Cost: Director, employees and one-shot calls, against the E-7 caps | |
| The deliverable: what it is, where it is, and does it run | |
| What went wrong or felt wrong (feeds risks #7 and #8, and `NEXT-VERSION` §B.6: whether a prompt composer was ever wanted) | |

Status: OPEN

---

## §E: Decisions for Nikunj

Recommendations are pre-filled. A session proceeds on the recommendation if
Decision is empty, and writes "recommendation followed".

| ID | Question | Recommendation | Decision |
|---|---|---|---|
| E-1 | Which Claude Code version is the pin? The dev box runs 2.1.276, CI and `TESTED_ENGINE_VERSIONS` say 2.1.238 | **Move both to 2.1.276**, dropping 2.1.238. The gate and the `--bare` proof run on the dev box's CLI, so testing 2.1.238 in CI would pin a CLI nobody runs. Claim only what is tested. Move only after the contract suite and the two opt-in tests pass on 2.1.276 (S1-4). Note that the CLI auto-updates, so record the version at the gate | Nikunj: recommendation accepted (2026-09-22) |
| E-2 | Where do the opt-in real-engine tests get the API key? Electron's safeStorage key lives in Bureau's own profile, so a Node test process cannot decrypt the key stored through Settings | **A key file outside the repo**, named by `BUREAU_TEST_ANTHROPIC_KEY_FILE` (default `%USERPROFILE%\.bureau-test\anthropic.key`, readable only by you). The helper stores it into the test's own database with `storeSecret` and the real broker injects it into the child. No process ever has `ANTHROPIC_API_KEY` set globally, so your own Claude Code sessions stay on your subscription. The helper refuses a key file whose ACL grants anyone but you (S1-6), and never logs or echoes the key | Nikunj: recommendation accepted (2026-09-22) |
| E-3 | Attachments (`NEXT-VERSION` §L.2): compose them into the outbox body at send time, or fold them into the Director's context? | **Fold them in during context assembly (S1-17).** The trigger queue composes the turn from the conversation row anyway, so the outbox body stays a plain copy and there is one reader. An attached file inside the project is readable through `Read(${project}/**)`. One outside it is named, and the Director is told it cannot open it. This depends on S1-11b | Nikunj: recommendation accepted (2026-09-22) |
| E-4 | §12.4: a memory proposal made **after** a phase's review was answered: join the next phase's batch, or raise a new review now (today's behaviour)? | **Join the next phase's batch.** The 14-day auto-reject clock starts when the batch is **raised**, not when the note is proposed, so nothing expires unread. Leftovers from the last phase are raised at delivery | Nikunj: recommendation accepted (2026-09-22) |
| E-5 | §10.6 rule 6: does Bureau itself ever push? | **No, not in v1.** Delivery is rule 5's local merge. Rule 6 stands as N-9's detector (any push is found and needs approval after the fact), and §10.6 records that there is no Core push path. A push feature, with auth and remotes, is future scope | Nikunj: recommendation accepted (2026-09-22) |
| E-6 | How do intake questions reach the user? §7.9 has no tool for it | **`bureau_report` gains `kind: 'question'`** with `payload.questions[]` (2–4 items, each with text, options and a recommendation), rendered by M9's question chips. It is a §7.9 amendment with a `§0.1` row, and it keeps the tool count at 19 | Nikunj: recommendation accepted (2026-09-22) |
| E-7 | Bureau's budgets for real runs. **With the default $2 Director reserve, a $1 daily cap leaves non-Director employees nothing**: their ceiling is daily − reserve, which is negative | **For S1's runs:** `dailyUsd` $1, `directorReserveUsd` $0.25, `perTaskUsd` $0.25. **For the gate:** `dailyUsd` $3, `projectUsd` $3, `directorReserveUsd` $0.50, `perTaskUsd` $0.50. The $5 prepaid balance is the hard backstop | Nikunj: recommendation accepted (2026-09-22) |

---

## §F: Found while fixing

| Found by | What | Proposed owner | Fixed now? |
|---|---|---|---|
| Planning, 2026-09-22 | Pre-M11 X-20 (`0bbd4c8`) shipped `HelperKeyField`, which calls `settings.setSecret`, a `stub('M13')`. Its renderer test mocks IPC, so saving a helper key has never worked. `getSecretsStatus` also lists only `anthropic_api_key` | M11 | Yes, in S1-5 (the Anthropic key field needs the same handler) |
| S1-12, 2026-09-22 | **S1-12 was too large for one commit**: seven tools plus the whole Director tool surface. Split into **S1-12a** (the surface, and `bureau_report`) and **S1-12b** (`bureau_get_project_state`, `bureau_write_memory`, `bureau_search_workspace` with their handler-side guards). Same Done-when between them, nothing dropped | This plan (split) | Yes — S1-12a is committed; S1-12b is the next OPEN row |
| S1-5, 2026-09-22 | **This plan's row IDs collide with the security-test numbers.** `securitySuiteCoverage.test.ts` reads `S(d{1,2})` from test code outside comments, so a title like `(M11 S1-1)` or a string like `S1-5` reads as a claim of security test S1, and the unit suite fails until `test:security` runs the file. S1-1's commit left the unit suite red this way; S1-5's commit fixed it. **Practice for every M11 row: the row ID goes in comments only, never in a test title or a string** | This plan (practice) | Yes, S1-5 (the three titles and one string) |
| Planning, 2026-09-22 | Settings accepts a `directorReserveUsd` greater than or equal to `dailyUsd` or `projectUsd`, which silently sets every non-Director employee's ceiling to zero or below. §8.0 wants this consequence surfaced in the Settings UI | M14 (settings completeness); E-7 avoids it for M11's own runs | No |
| Review of S1, 2026-09-23 | S1-9 contains **every engine process that production spawns today**, which is only the Director's, because `startDirector` is the sole production caller of `spawnSupervisedEmployee`. The employee spawn path built in M12 must pass `containProcess` the same way, or the row's claim quietly stops holding (§10.3.1; the adapters contain nothing when none is injected) | M12 (hiring and the assignment loop) | No |
| S1-4, 2026-09-23 | **The E-2 key is org-scoped, so every real run 400s before it bills anything.** With the key file in place, `realEngineSpawn` reached the real API and got `this API key is not scoped to a workspace, so this request must include the anthropic-workspace-id header`. A direct `/v1/messages` call with the same key returns the same 400, so this is the key, not Bureau. It blocks S1-4's two runs, S1-6's remaining run, S1-7's `--bare` proof and the §GATE. **Nikunj: create the key inside a Console workspace** (or add a workspace to the existing one) and write it to `%USERPROFILE%.bureau-testanthropic.key` with the same ACL; nothing in Bureau changes. Passing `anthropic-workspace-id` from Bureau instead would be a new code path no row asks for, and is not done here | Nikunj (unblocks M11 §S1) | **Resolved 2026-09-23 by Nikunj:** the workspace-scoped key replaced it, and both real runs went through with it — `realEngineSpawn` $0.1356 and `realAgentGate` $0.0672 (S1-4, S1-6), then S1-7a’s `--bare` measurement ($0.0093). Nothing in Bureau changed |
| S1-12b, 2026-09-23 | **The §F S1-5 practice needs a test, not a practice.** It was broken three times in one session — by S1-4, by this row and by S1-7a — because a row ID in a `describe` title is the natural thing to write and nothing refuses it until the whole unit suite runs. `securitySuiteCoverage` already knows how to find these; it could name the plan-row shape (`S<digit>-<digit>`) as a distinct, refused case with a message saying to move it into a comment, instead of reporting it as a missing security test | M11 (any session that touches `securitySuiteCoverage`), else M15 | No — the two titles were fixed in S1-12b's commit |
| S1-4, 2026-09-23 | **A real run with no `modelId` runs the CLI’s default model, not the role’s tier.** `realEngineSpawn` cost $0.1356 against the plan’s ~$0.05 estimate, because its context is hand-built with `modelId: null` — only `Supervisor.assign()` resolves the tier (§7.5), and that test has no Supervisor. Harmless for one short turn, but it means the contract suite exercises a different model from the one an employee would actually run under, and the gate’s estimate is built on the same arithmetic. Worth either resolving the tier in the helper or saying in the row that this run is deliberately un-tiered | M11 §GATE (before the budget is set), else M15 | No — recorded, and both S1 runs stayed well inside E-7 |
| S1-21, 2026-09-24 | **`{ ...process.env, NAME: v }` is not a replacement on Windows.** Env var names are case-insensitive there, object keys are not: if the snapshot spells it differently the child gets both and the pre-existing spelling wins. Measured — Vitest's `process.env` carries `PSMODULEPATH`, and S1-21's fix was silently inert under it until the helper stripped case variants. Every other spread-and-override of an environment in `src/` has the same hazard, unaudited | Post-M11 audit (M7–M11) | No — fixed only in `windowsPowerShell.ts`, which is the file S1-21 was already changing |
| S1-21, 2026-09-24 | **Tests still call `icacls` and `powershell.exe` off PATH.** Production no longer does (S1-21), and `powerShellSpawnsGoThroughOneHelper.test.ts` scans `src/` only. A `tests/` spawn that resolves through PATH can fail for a reason that has nothing to do with what is being tested — which is how S1-21 started | Post-M11 audit (M7–M11) | No — out of the row's "Done when", which names `src/` |
| S1-7b, 2026-09-24 | **S1-7a's premise for the handshake was wrong: `--max-turns 0` does not stop the CLI before a model call.** Measured on 2.1.276 with no usable key: `num_turns: 1`, the model call attempted (`terminal_reason: api_error`). A `SessionStart` hook answering `continue: false` does not stop it either. With a valid key, the handshake as the row describes it would have billed a turn on every start. The row's premise, §7.6 and risk #34 all carried the claim | This plan (S1-7b) | Yes, in S1-7b: the handshake gets no credentials (the broker is never asked) and an unreachable `ANTHROPIC_BASE_URL`, so it is free whether or not the hook runs. The free test asserts the key is absent even though the broker holds one. §7.6 and risk #34 corrected |
| S1-7b, 2026-09-24 | **Hook liveness is checked per start, not per turn.** The CLI auto-updates, and every structured turn is a fresh `claude -p`. A mid-session update to a hook-skipping default would run ungoverned turns until the employee next starts. Each turn already fires `SessionStart`, which the endpoint receives and ignores. Closing this needs the Core to tie a `bureau_` tool call to a turn whose report arrived, and the MCP call doesn't carry that today | M12 (employees run for hours there), else the post-M11 audit | No — outside the row's "Done when" ("the Supervisor assigns no work until it arrives") |
| S1-13, 2026-09-24 | **A chat message a tool handler posts is written but never pushed to the window.** `bureau_report` calls `appendChatMessage` with no broadcaster, because a `ToolHandler`'s context has none. The row lands and the event is emitted, but an open chat shows the card only after its next re-hydrate. The Director's streamed prose is pushed, because it goes through `main()`'s `ChatStreamRegistry`. Every card tool to come has the same gap: `bureau_write_brief`, `bureau_write_plan` and E-6's questions. S2-7's e2e ("a brief card appears, the user clicks Approve") depends on this | M11 §S2, before S2-7 (the chat broadcaster threaded into the control channel's handler context, as `messages/router.ts` already receives it) | **Moved to S2-0, done there** (was: No — the control channel's handler context is not a file this row changes) |
| S1-15, 2026-09-24 | **The Director could never receive a message in production.** Two `Supervisor` defects, each invisible to FakeAdapter tests that script an `idle` event. (1) After `assign()`, a structured employee with no task stays `starting`: no process runs until a turn is sent, and nothing but an engine event ever moved it. The router (and now the queue) delivers only to an `idle` employee. (2) A completed turn with no task went to `blocked: ended_without_report`, so after its first turn the Director was held forever. The real `ClaudeCodeAdapter` emits no `idle` event, and FakeAdapter did not end a turn on `finished`, which is why no test saw either | This plan (S1-15) | Yes, in S1-15: both are in `supervisor.ts`, which the row changes, and together they broke the row's own "waits for idle". Each has a mutation that fails the integration test. FakeAdapter now ends a turn on `finished` |
| S1-17, 2026-09-24 | **Appendix A.2's `{{user_name}}` has no source.** No table or setting stores the user's name, so the Director's prompt says "the user". Onboarding is where a name would be asked for | M13 (onboarding), else M14 (settings) | No — nothing to read it from |
| S1-17, 2026-09-24 | **`claudeCodeAdapterProbe.test.ts`'s "hanging binary" case can fail by 1 ms.** It asserts the elapsed time is `>= 30,000` ms against a 30 s timer. Under a full packaged run it measured 29,999 ms, and alone it measured 30,007. Node timers and `Date.now()` do not agree to the millisecond, so the assertion needs a tolerance of a few ms, or a monotonic clock on both sides | Post-M11 audit (M7–M11) | **Moved to S1-22, done there** (was: No — not a file this row changes) |
| S1-19, 2026-09-24 | **`fireAndRehire.test.ts`'s "a caller who names someone new gets a new person" failed once in a full packaged run and passed alone (11/11).** The failure: `expected '01M3…' not to be '01M3…'`, meaning hiring returned the archived employee's id when a new name was given. S1-19 touches neither hiring nor ids. It is intermittent, and whether the cause is the name pool, the rehire lookup or id generation is unknown | Post-M11 audit (M7–M11), else M12 (hiring is exercised there) | **Moved to S1-22, done there** (was: No — not a file this row changes, and not reproduced) |
| S1-14, 2026-09-24 | **Rule 11 broken once this session.** S1-15's `src/` edits were made while S1-14's packaged integration run was in progress. The run was stopped as invalid, and the full suite was run once over both rows (recorded on S1-15) | This plan (practice) | Yes — noted in S1-14's status, not hidden |
| S2-1, 2026-09-25 | **The row and §5.1 disagree on which conversation a new project gets, and the choice shapes §S2.** The row: "one transaction creates the project (stage `intake`) **and its conversation**". §5.1 (BUILD-SPEC, `conversations`): "there is always one company-level conversation … `bureau_set_project_stage` creates a `projects` row with `stage='intake'` and **binds the current conversation** to it." Creating a new one leaves the user's request, and the thread's history and context, in the old one, so something has to carry them over. Binding the current one keeps them, but then a new company-level conversation is needed to keep §5.1's "always one". **Either way, "the conversation the Director is in" can no longer be "the most recently created"**: `resolveConversationForDelivery(db, null)` decides it today in `bureau_report`, the trigger queue's intent step and context assembly, so the turn has to carry its conversation. **Recommendation: bind the current conversation** (§5.1), create the fresh company-level one in the same transaction, and have the Director's turn carry its conversation id | Nikunj | **Decided by Nikunj, 2026-09-25:** bind the current conversation to the new project (§5.1), create the fresh company-level conversation in the same transaction, and have every Director turn carry its conversation id. The switcher is a list, not tabs: the company conversation plus one entry per project, each with its status and an unread or waiting marker. Isolation proved with two deliberately similar projects (two restaurants). **Split into S2-1a/b/c** (the S1-12 precedent: too large for one commit; nothing dropped). Three Director behaviours from the same decision go to S2-2 (§F below) |
| S2-1 decision, 2026-09-25 | **In the company conversation, a message that could mean more than one project gets one question with those projects as options; one match gets no question (invariant #9).** Needs E-6's question shape, which S2-2 builds. Acting on a project from the company conversation also needs the Director's project-scoped tools to take an explicit project, since that conversation has none — and whenever it acts on one there, **it names the project it chose** | M11 S2-2 (the question shape and the prompt arrive there) | No |
| S2-1 decision, 2026-09-25 | **In a project's conversation, a question about another project gets a short answer and an offer to open that project's conversation.** A prompt rule, plus a one-line roster of the other projects (name and stage, never their brief, plan or memory) for the short answer, plus the offer's "open" affordance | M11 S2-2 (the prompt) | No |
| S2-1 decision, 2026-09-25 | **New work described inside a project's conversation is offered as a new project.** The plain-code half is S2-1b (nothing is created; the turn says so; an accepted offer goes through `bureau_set_project_stage`). The prompt's wording of the offer is S2-2's | M11 S2-2 (wording); S2-1b (mechanism) | No |
| S2-1a, 2026-09-25 | **A project's memory pack could be composed from another project's notes.** `composeMemoryPack` searched the `project` scope with no project filter, so any project's note matching the task's words could join the pack — employees' packs (M10) as well as the Director's context. Found by the isolation test, not by looking | This plan (S2-1a) | Yes, in S2-1a: `searchMemory` gained `projectScopeRef` and both pack searches pass the pack's project. The row's Done-when ("assembles only that project's … memory") could not hold without it |
| S2-1a, 2026-09-25 | **The `role` scope has the same shape.** The pack's search includes `role` notes with no role filter, so another role's playbook can match a task's words. Pinned role notes are filtered (`listPinnedMemory(db, 'role', roleRef)`); the search is not. Not a project leak, so not this row's | Post-M11 audit (M7–M11), else M12 (roles multiply there) | No — outside the row's Done-when |
| S2-1a, 2026-09-25 | **Existing conversations have no session of their own yet.** Before S2-1a only compaction wrote `conversations.director_session_id`; the Director's session lived on its employee row. So the first turn in each such conversation after this change starts a fresh engine session, seeded by context assembly (summary and recent turns), rather than resuming. Nothing is lost from Bureau's records; the engine's own transcript is | Recorded only (pre-release: no user has such a conversation) | No |
| S2-1b, 2026-09-25 | **A project created from the chat has kind `mixed` until something says otherwise.** Kind is one of §8.1's intake dimensions, so it is not known at creation, and `projects.kind` is NOT NULL. Nothing yet sets it from what intake learns | M11 S2-3 (the brief carries the kind; approval can write it) | No |
| S2-1b, 2026-09-25 | **A chat-created project's folder is a path, not a folder.** `createProject` picks `<home>/<slug>` and creates nothing on disk, and intake may still learn the work belongs in an existing folder (§8.1's "Existing assets"). Where the folder is created, or the path is changed to an existing repository, is not decided by any row | M11 §S3 (the first assignment needs a workspace), else M13 (onboarding's folder scanner) | No |
| S2-1b, 2026-09-25 | **`projects.open` opens a project's conversation; it does not reopen a delivered project.** §8.7's "reopening creates a new brief version" is a different operation that shares the word | M11 §S3 (delivery), else M14 | No |

---

## §G: Final sweep

One suite at a time, with no `src/` edits while a packaged suite runs:
`format:check`, lint, typecheck, the four `check:` scripts, unit,
`npm run package` then integration (staleness gate confirmed by name),
contract, e2e, `test:security` with both runs reported, and `test:coverage`
recorded against the pre-M11 baseline. Then Nikunj pushes, and CI must be
green.

---

## §H: Coverage check (last step, bookkeeping only)

1. **No open rows.** `grep -cE '^\|.*\| OPEN \|$' docs/M11-PLAN.md` returns `0`.
2. **Every decision is made.** No empty Decision cell in §E.
3. **Every DONE has a real commit.** `git cat-file -t <hash>` returns `commit` for each.
4. **The source ledger holds.** Every §S row's items appear here with a resolved status.
5. **No M11 stubs.** `grep -rn "stub('M11')" src/` returns nothing, and each of the 14 is built by its row or MOVED with a reason in §F. (`NEXT-VERSION` §D.1's close-out rule.) Also, `settings.setSecret`/`clearSecret`'s `stub('M13')` are gone.
6. **No dormant M11 settings.** `grep -rn "inactiveUntil: 'M11'" src/` returns nothing, and `settingsNotYetActive.test.ts` is green.
7. **`PROJECT-CHECKLIST.md` agrees.** The M11 row, risks #7, #8, #9, #10 and #16, and the three 2026-09-22 Known Issues rows state their real status. Standing rule 8 is applied to any audit finding touched.
8. **`NEXT-VERSION.md` agrees.** §B.6, §D.2, §H.1, §H.6, §I.3, §J.3, §J.5, §K.1, §K.2, §L.1, §L.2, §L.4, §M.4, §M.5 and §N.5 each carry their updated status.
9. **§F is settled.** Every line has an owner, and any line fixed now has a commit.
10. **The gate record is complete**, and the spend total is written in it.

---

## §S: Source ledger

| Source | Items | Where |
|---|---|---|
| **A.** §28 M11 items 1–15, and the gate | 1 → S1-11, S1-8 · 2 → S1-17 · 3 → S1-18 · 4 → S1-15 · 5 → S1-14 · 6 → S1-16 · 7 → S2-1, S2-2 · 8 → S2-3 · 9 → S2-4 · 10 → S3-2 · 11 → S3-4 · 12 → S3-3, S3-5, S3-6 · 13 → S1-19 · 14 → S3-11 · 15 → S3-7 · gate → §GATE |
| **B.** `PRE-M11-PLAN.md` §M11 items 1–15 | 1 → S3-5 · 2 → S1-8, S3-2 · 3 → S3-2 · 4 → S2-7 · 5 → S1-12, S3-10 · 6 → S1-15 · 7 → S1-20 · 8 → S3-3 · 9 → S2-5 · 10 → S1-17 (attachments, A.2 slots), S2-3 (`requestEdit`), S1-12 (memory tools), S3-1 (Appendix B) · 11 → S1-16 · 12 → S2-1 · 13 → S2-2 · 14 → S2-6 · 15 → S1-7 |
| **C.** `PRE-M11-PLAN.md` §F lines owned by M11 (nine) | exit flush → S1-10 · `boundCommand` → S3-1 · worktree deleted mid-session → S3-2 · clean merge moves checked-out `base_ref` → S3-4, S3-5 · version drift → S1-3 · `containProcess` → S1-9 · adapter factory → S1-8, S1-6 · six non-reversible queues → S3-8 · risk #10 → S3-4 |
| **D.** Known Issues, 2026-09-22 | SID match → S1-1 · `expect(true)` → S1-2 · version pin → S1-4, E-1 |
| **E.** Risk rows owned by M11 | #7 → S2-2, S3-11 · #8 → S2-2, S3-11 · #9 → S2-4 · #10 → S3-4 · #16 → S1-18 |
| **F.** §19 Director behaviour | S3-11 |
| **G.** `NEXT-VERSION.md` sections owned by M11 | §B.6 → §GATE record · §D.2 → S3-5, S3-9 · §H.1 → S1-16 · §H.6 → S1-8, S3-1 · §I.3 → S1-20 · §J.3 → S3-3 · §J.5 → S2-6 · §K.1 → S1-13 · §K.2 → S2-1 · §L.1 → S2-7 · §L.2 → S1-17, E-3 · §L.4 → S2-3 · §L.5 → no structured plan editor wanted; `plan.requestEdit` → S2-3 · §M.4 → S1-12, S2-2 · §M.5 → S3-1 · §N.5 → S2-5. Not M11's: §N.3 (M15, unless a slow reindex is seen) |
| **H.** Engine API-key setup | key field → S1-5 · tests through the broker → S1-6, E-2 · pin → S1-4, E-1 · `--bare` → S1-7 |
| **I.** Spend, $5 prepaid | rule 12, E-7, the cost column of each real-run row, §GATE |
| Nikunj's plan review (2026-09-22), five changes | tool-call bridge → S1-11a · Director's `${project}` → S1-11b · hook liveness → S1-7 · key-file ACL → S1-6, E-2 · premises → S1-8, S1-13 |
| Already decided (the prompt) | EmployeeContext → S3-1 · model in `assign()` only → rule 7, S3-1 · E-4 terms → S1-5, S1-7 · the lease guarantee → S3-2 · Director autonomy fixed (X-7) → unchanged, S1-8 relies on it |
| Code: `stub('M11')` (14) | `projects.create`/`open` → S2-1 · `brief`/`plan.requestEdit` → S2-3 · `phases.*` ×3, `deliverables.*` ×3 → S3-5 · `projects.pause`/`resume`/`abandon` → §H check 5 (built where a row reaches them, otherwise MOVED in §F) |
| Code: `inactiveUntil: 'M11'` (10 settings) | `director.*` → S1-15, S1-17, S1-18 · `intake.maxRounds` → S2-2 · `reporting.heartbeatMinutes` → S1-15 · `orchestrator.*` → S3-2, S3-6 · `review.*` → S3-4 |

