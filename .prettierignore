node_modules
dist
dist-package
.tsbuildcache
native/*/build

# Generated analysis output, like dist/: written by a tool, read by a person,
# formatted by neither. Untracked, and not this project's source.
graphify-out
docs/artifacts
docs/
package-lock.json

# Hand-authored project documents. They live outside docs/ by accident of
# layout, not because they are a different kind of file: every session appends
# to them, and PROJECT-CHECKLIST.md has table cells thousands of characters
# long. Reformatting them means permanent churn in the files that change most
# often, for no benefit.
/PROGRESS.md
/PROJECT-CHECKLIST.md
/HOW-IT-WORKS.md

# Shipped content, not code. Role prompts are fed to a model, where line breaks
# are an authoring decision; pack YAML holds tool patterns like
# "Bash(npm *|pnpm *)" and is validated by M7's own pack validator. Their
# formatting is a content decision, not a style one.
/packs/

# Project skills are instructions read by a model, the same kind of content as
# role prompts above: line breaks and emphasis are authoring decisions.
.claude/skills/
